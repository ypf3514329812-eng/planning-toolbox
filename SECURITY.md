# Security Policy

Planning Toolbox 默认是本地优先软件。MCP 桥接只应监听本机回环地址，并使用随机令牌、命令白名单和路径白名单。

## 不要公开提交

- API Key、访问令牌、私钥或密码；
- `%APPDATA%\PlanningToolbox` 下的 MCP 配置和日志；
- 真实 CAD、GIS、图片、SKP 或项目文件；
- 包含个人信息、坐标位置或项目名称的测试证据。

## 报告安全问题

请不要在公开 Issue 中发布可利用的凭据、用户文件或远程利用细节。公开仓库启用 GitHub Private Vulnerability Reporting 后，请优先通过该入口提交；在此之前，请先开一个不包含敏感内容的 Issue，要求维护者提供私下沟通方式。

安全问题应包含受影响版本、复现步骤、影响范围和建议修复方向。不要为了复现而上传正式项目数据。

## 安全边界

本项目不接受任意 Ruby、Shell 或远程命令执行能力。MCP、路径白名单、源文件零修改和 CRS/单位阻断属于安全边界，任何修改都必须有回归测试和审计说明。
