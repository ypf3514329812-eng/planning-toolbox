# Planning Toolbox Test Report（当前工作树：v0.60.0 质量关卡版）

> 当前验证日期：2026-08-11。当前工作树基于 Git `5e607c3`，本轮为 v0.60.0 全链路质量关卡与中文复核清单增强；因此测试结果代表本机工作树，不等同于旧标签 `v0.6.0-desktop-ui`。

## Current Overall 状态

**WORKING BUILD / 264 TESTS PASS / QUALITY GATE UI PASS / ARCGIS REAL INTEROP PASS / SKETCHUP 2026 MCP RUNTIME PASS**

- 完整命令：`python -m pytest -q`
- 最新结果：`264 passed in 23.10s`。新增“需复核/已阻断”界面质量关卡回归；既有 CAD、GIS、图转 CAD、GUI、SU 交接和 MCP 测试全部通过。
- 真实 SketchUp 2026 桥接验证：`test_artifacts/mcp_bridge_runtime_v0581/sketchup_mcp_bridge_report.json` 为 PASS；8765 返回 v0.58.1 健康状态，测试实例自行退出后配置文件、监听端口和 SketchUp 进程均为 0 残留。
- v0.58.1 RBZ：`test_artifacts/mcp_bridge_runtime_v0581/PlanningToolbox_SketchUp_Importer_v0581.rbz`，1,002,340 bytes，18 个成员，SHA-256 `65622B9118D67826EDFDAA6EFC59E655262D4C7C14A74223D9D960F32AF70CD3`。
- v0.60.0 桌面目录包构建成功：`dist/PlanningToolbox/PlanningToolbox.exe`，23,128,292 bytes，SHA-256 `C5C00DA20073B43F748186756ACB38480AB5636DA31FB331BEA1613AF2076CE0`；桌面快捷方式仍指向该文件。
- v0.54 上一轮结果：`249 passed in 33.09s`（保留作为道路去重批次的历史对照）
- Exit code：`0`
- GUI/EXE：PyInstaller 目录版构建成功。桌面成品受控启动 8 秒后仍可响应，工作集约 135.6 MiB / 私有内存约 70.8 MiB，随后仅关闭本次测试实例。当前 EXE 本体 23,128,292 bytes，目录包 672,230,938 bytes（695 个文件）。
- EXE SHA-256：`C5C00DA20073B43F748186756ACB38480AB5636DA31FB331BEA1613AF2076CE0`（23,128,292 bytes）。
- 原始 DXF：所有自动修复只写新副本，并执行 SHA-256 零破坏校验。
- 外部 AutoCAD 最终人工显示验证仍为 **PENDING USER VALIDATION**；ArcGIS Pro 3.7 数据互操作，以及 SketchUp 2026 的弯道、环岛、中心线道路带和本轮真实图片全链路插件导入、SKP 保存、PNG 导出均已通过。SketchUp 2026 的无模型欢迎页不会执行 `-RubyStartup`，验收脚本固定使用只读验证模型作为启动引导，并强制加载当前测试插件，避免旧版用户插件污染证据。

## 历史验证：100 MB 内的规划组件与参考模式资源包（v0.58 验证批次）

- 组件目录从 9 个扩展到 14 个：新增停车车辆、公共座椅、灌木组、道路隔离柱和公交候车亭。5 个新增对象均保留 CAD 块旋转角，使用共享 SketchUp 定义；运行时优先使用项目原生过程式生成器，兼容 SKP 快照只作备用。
- 资源量化：内置组件 1,220,468 bytes；组件源资产 203,549 bytes；参考模式索引 13,912 bytes；三者合计约 1.37 MiB，低于 100,000,000-byte 硬预算。参考索引收录 22 类规划场景，只存来源页面、参考图检索词、几何要点和复核清单，不嵌入图片、纹理、模型权重或网络依赖。
- 真实 SketchUp 2026 验收 PASS：5 个原生定义、13 个组件实例（含 8 个既有街灯实例）、模型保存成功，SKP 228 KB，预览 PNG 36 KB，零孤立对象。证据目录：`test_artifacts/native_component_runtime_v058/`。
- RBZ：`test_artifacts/native_component_runtime_v058/PlanningToolbox_SketchUp_Importer_v058.rbz`，996,058 bytes，17 个成员，SHA-256 `0982E020C73F7CCE68828E0C6EF0D4EDB69E831731C5AB0D8C0CCBBD3769DBFE`。
- 已同步到 SketchUp 2026 用户插件目录：安装目录含 14 个 SKP；`main.rb` 与源文件 SHA-256 均匹配，当前打开 SketchUp 后可直接使用新组件。

## 当前增量：图转 CAD 候选边界对齐质量量化（v0.45 验证批次）

- 黑白图转 CAD 现在为建筑与道路候选分别计算边界平均偏差、P90 偏差、最差对象均值和状态；这是审查指标，不会自动篡改候选几何。
- 真实 1076×1462 底图验证：30 个建筑候选平均偏差 0 px、状态 aligned；12 个道路候选平均偏差 3.478 px、P90 12.042 px、状态 review_required。系统会提示先查看叠加图，再决定是否进入 CAD/SU。
- 结果区新增“原图边界对齐”行，帮助非编程用户快速区分“坐标整体对齐”和“道路候选需要人工修边”两种问题；证据目录：`test_artifacts/alignment_quality_v045_20260810/`。

## 当前增量：道路中心线候选接力（v0.46 验证批次）

- 黑白图转 CAD 在保留 `BW_ROAD_CANDIDATE` 道路面候选的同时，新增独立的 `BW_ROAD_CENTERLINE_CANDIDATE` 开放多段线层；这让 SketchUp 可以在用户开启“中心线道路带”后获得道路方向和概念宽度的接力输入。
- 真实 1076×1462 底图验证：输出 12 个道路面候选和 15 条道路中心线候选，DXF 图层实体数量一致；源图 SHA-256 前后均为 `492fb27390850407e4bb77fae0ab6364dc2a0a4d7ff7b1565903ffcdfde7e0c2`。
- 新增 2 项回归：DXF 中心线为开放实体、SketchUp handoff 能将 `BW_ROAD_CENTERLINE_CANDIDATE` 识别为 `centerline_corridor`。道路边界仍按 v0.45 为 `review_required`，不会自动宣称为最终道路红线。证据目录：`test_artifacts/road_centerline_candidates_v046_20260810/`。

## 当前增量：道路诊断与逐中心线宽度接力（v0.47 验证批次）

- 黑白图转 CAD 新增道路专用叠加复核图：蓝色表示道路面候选，橙色表示道路中心线候选，用户可在进入 SketchUp 前快速判断漏检和误检位置。
- 每条 `BW_ROAD_CENTERLINE_CANDIDATE` 现在写入 `PT_ROAD_WIDTH_M` DXF XDATA。SketchUp 开启“中心线道路带”且总宽填写 0 时，会优先读取每条中心线的检测宽度；用户明确填写总宽时，仍以用户值覆盖，保持可控。
- 真实底图验证：15 条中心线全部带有宽度元数据，检测宽度为 4.892–21.200 m；由于底图包含多种道路等级，系统标记为 `mixed_widths`，不再提供误导性的单一统一宽度。SketchUp 自动宽度接力 15/15 条成功。证据目录：`test_artifacts/road_diagnostics_v047_20260810/`。

## 当前增量：道路中心线可信度复核（v0.48 验证批次）

- 每条道路中心线现在写入 `PT_ROAD_CONFIDENCE` 和 `PT_ROAD_CANDIDATE_ID` DXF XDATA，并在 SketchUp 交接 JSON 中保留 `centerline_confidence` 与 `centerline_review_required`。
- 真实底图验证：15 条中心线中 12 条达到概念建模阈值（可信度 ≥ 0.65），3 条被标记为需复核；SketchUp 仍成功生成 15 条中心线道路带，不会因为低可信候选而静默丢失对象。
- 道路叠加图图例：橙色为概念建模可信，红色为优先复核。该标记是质量提示，不是审批结论。证据目录：`test_artifacts/road_confidence_v048_20260810/`。

## 当前增量：一键道路修正入口（v0.49 验证批次）

- 图片转 CAD 结果区新增“🖍️ 修正道路”按钮。用户点击后，系统自动切换到图片转 CAD 的语义引导模式，带入原图和已生成的同像素引导草稿，并直接打开道路路径编辑器。
- 编辑器保存后仍需用户点击“运行”，系统不会自动覆盖原始图片或原始 DXF；补画道路只会生成新的引导图、DXF 和交接文件。
- 新增 GUI 回归覆盖按钮发信号、主窗口自动切页/带入路径和 960px 窄屏无横向溢出。全量测试为 242/242。该入口降低了道路漏检后的操作步骤，但仍不能代替道路红线和规范复核。

## 当前增量：同像素道路复核叠加层（v0.50 验证批次）

- 黑白图转 CAD 额外生成与原图完全同像素尺寸的道路复核图；蓝色表示道路面候选，橙色表示较高可信中心线，红色表示需优先复核中心线。
- 一键道路修正打开编辑器时，该复核图作为独立显示层叠加在原图/引导图之上；它不会写入可识别的语义颜色，也不会参与 CAD 转换。
- 真实 1076×1462 底图验证：原图、语义引导图、道路复核图三者尺寸完全一致；保存编辑后的语义引导图，提示层像素不会混入输出，原图 SHA-256 保持不变。全量测试为 244/244，证据目录：`test_artifacts/road_review_overlay_v050_20260810/`。

## 当前增量：图转 CAD 直达 SketchUp（v0.51 验证批次）

- 图转 CAD 结果现在提供“继续生成 SketchUp”按钮；结果对象新增稳定的 `dxf_file` 路径，旧结果也会从输出文件列表兼容回退查找 DXF。
- 直达入口只切换当前工作 DXF 和 SketchUp 参数页，不会静默运行；用户仍需确认楼层、层高、道路预设和概念道路宽度。
- 真实底图验证：输出 30 个建筑候选、16 个道路面候选、24 条道路中心线候选，其中 8 条需优先复核；直达接力生成 30 个六层建筑、1 个锁定原图底图组、1 个锁定矢量参考底图组和 24 条中心线道路带提示。原始输入图 SHA-256 保持不变，证据目录：`test_artifacts/direct_sketchup_handoff_v051_20260810/`。
- 新增 2 项 GUI 路由回归，完整测试为 246/246；中心线自动接力只针对显式 `BW_ROAD_CENTERLINE_CANDIDATE` 图层，不会把普通线条强行解释为道路。
- 本轮桌面包已重建：EXE 23,077,663 bytes，SHA-256 `57DC21E909AE9918906284F563E7F15971688103CC8AD37B6895D1992747B2F3`；688 文件目录包 671,253,630 bytes。启动响应、第二实例 code 0 退出和测试后无残留进程均通过。

## 当前增量：低可信道路实体质量门（v0.52 验证批次）

- SketchUp 页新增道路候选可信度策略。“仅高可信候选生成实体（推荐）”会把置信度低于 0.65 的图像识别道路保留为中心线线索，不生成有宽度的道路实体；“全部候选生成实体”保留旧行为，方便逐条人工复核。
- 该质量门只约束带 `PT_ROAD_CONFIDENCE` 的图像识别候选。手工 CAD 中明确命名为道路中心线但没有识别置信度的对象仍按用户选择生成，不会被误拦截。
- 真实底图 A/B 验证：24 条中心线候选中 16 条高可信候选生成道路带，8 条低可信候选降级为待复核中心线，高风险道路实体减少 33.3%；30 栋建筑、锁定底图与 936 个源对象保持不变，原 DXF SHA-256 `8b774402d159eb53044e4aa7e1a9458820ff5d98d12a5d7a83b30c3943f685de` 零修改。
- 完整测试为 247/247；证据目录：`test_artifacts/road_confidence_gate_v052_20260810/`。
- 本轮桌面包已重建：EXE 23,079,599 bytes，SHA-256 `8AFF3638D079C0ADCCA1F47B6B9D9BF76A6677735804D1A1B76224CE45E730BD`；688 文件目录包 671,255,566 bytes。启动响应、第二实例 code 0 退出和测试后无残留进程均通过。RBZ 保持 338,391 bytes、12 个成员，SHA-256 `031020D4BA62A07C64781A321D24609F26121E5978791D05CF88412D29726624`。

## 当前增量：道路中心线网络保守拼接（v0.53 验证批次）

- 黑白图道路候选会在写入 DXF 前整理中心线网络。只有可信度不低于 0.65、检测宽度相差不超过 25%、端点方向转折不超过 22.5°且桥接间隙受图片分辨率与道路宽度共同限制的短段才允许拼接。
- 低可信候选完全不参与拼接，宽度不同或明显转弯的路段保持独立；拼接后的宽度与可信度按原线长度加权，不会把低可信线“平均”为高可信线。
- 真实底图 A/B 验证：24 个原始中心线碎段整理为 21 条 DXF 中心线，安全拼接 3 个碎段，最大桥接间隙 4.4847 m；16 个高可信道路片段成为 13 条连续 SU 道路带，8 条低可信线仍全部保留为待复核对象。30 个建筑候选、16 个道路面候选及源图 SHA-256 均保持不变。
- 完整测试为 248/248；证据目录：`test_artifacts/road_network_merge_v053_20260810/`。
- 本轮桌面包已重建：EXE 23,083,770 bytes，SHA-256 `9FE67D81B90F7C9059DA98AC7251C37DFAF71AC1F977D20314726608CE1DE39F`；688 文件目录包 671,259,737 bytes。启动响应、第二实例 code 0 退出和测试后无残留进程均通过。RBZ 保持 338,391 bytes、12 个成员，SHA-256 `031020D4BA62A07C64781A321D24609F26121E5978791D05CF88412D29726624`。

## 当前增量：图片道路面与中心线实体去重（v0.54 验证批次）

- 当图片转 CAD 同时输出 `BW_ROAD_CANDIDATE` 闭合道路面和 `BW_ROAD_CENTERLINE_CANDIDATE` 中心线，且用户使用“中心线道路带 + 仅高可信候选生成实体”推荐策略时，闭合道路面不再重复生成场地面、厚度、标线或街灯，而是保留为可见、可选择的复核轮廓。普通 CAD 道路面、`AI_ROAD` 和“全部候选生成实体”兼容模式不受影响。
- 同一张 1076×1462 真实用户底图经 SketchUp 2026 `26.2.243` 真机导入 PASS：16 个重复道路面全部转为无面的复核轮廓，13 条可信中心线继续生成道路带，8 条低可信中心线继续保留为复核线，30 栋六层建筑、196 棵共享树、2 个锁定底图组和源文件 SHA-256 均保持不变。
- 与 v0.53 同源模型相比，直接面数由 7,558 降至 5,496（减少 27.3%），直接边数由 32,838 降至 27,374（减少 16.6%），自动街灯由 124 降至 24（减少 80.6%），车道标线由 75 降至 33；重复道路面组的直接面数严格为 0。模型仍保留 13 条道路带，说明减少的是重复细节而不是核心道路功能。
- 完整测试为 249/249；真实 SKP、俯视图、轴测图与运行报告位于 `test_artifacts/road_surface_dedup_v054_20260810/`。RBZ 为 338,500 bytes、12 个成员，SHA-256 `90FDAAEA2DEF6807C3AD8436541990484AF51FF9CF573795B5AD8CA117B7E58A`，并已同步到 SketchUp 2026 用户插件目录。
- 本轮桌面包已重建：EXE 23,084,966 bytes，SHA-256 `5CC635B27938D704B33106ECA296E518FBF46973B5F61F38B7FF4DF6829258A0`；688 文件目录包 671,261,589 bytes。启动响应、第二实例 code 0 退出和测试后无残留进程均通过。

## 当前增量：CAD 低饱和语义显示与路口安全吸附（v0.55 验证批次）

- 黑白图转 CAD 为建筑、道路、道路中心线、树木/绿化和停车图层设置低饱和真彩色与 ACI 回退；建筑和道路半透明 HATCH 分别进入 `BW_BUILDING_FILL`、`BW_ROAD_FILL`，可在不隐藏边界多段线的情况下单独关闭，未确认线条降低为浅灰参考线。边界多段线仍是唯一几何依据，`line_count` 不再把显示填充误算为可编辑几何。
- 46 个显示填充均写入 `PT_PRESENTATION_FILL` XDATA。语义 sidecar 记录其数量但不把它们重复计入待复核对象，SketchUp 交接记录 `presentation_fill_ignored_count=46` 且 `skipped_count=0`，没有重复建模。
- 道路网络新增受限端点与 T 形路口吸附：只处理高可信、宽度相容、方向朝向目标且距离受限的候选；歧义匹配与低可信线保持原样。真实用户图因没有满足全部安全条件的新增断口，吸附数为 0，说明规则没有为追求数量而强行改线。
- 同一张真实用户图输出 30 个建筑、16 个道路、21 条中心线、196 棵树和 1 组停车候选；CAD 语义预览为 1768×2400 PNG。源图 SHA-256 前后均为 `492fb27390850407e4bb77fae0ab6364dc2a0a4d7ff7b1565903ffcdfde7e0c2`。
- SketchUp 2026 `26.2.243` 真机导入 PASS：30 栋六层建筑、196 棵树、13 条可信道路走廊、8 条待复核中心线、16 个无面的道路复核轮廓与 2 个锁定底图；直接面 5,496、直接边 27,374，SKP 和俯视/轴测 PNG 均成功保存。
- 完整回归 `252/252` 通过。桌面包 EXE 23,094,436 bytes，SHA-256 `A107B2AA6351F9A818C255CA6069F6073A9599E809E4A6B562415FA5FF58C3D2`；688 文件目录包 671,271,059 bytes。启动可响应，复测工作集 146,337,792 bytes、私有内存 78,147,584 bytes；第二实例 code 0 退出，关闭后无残留进程。证据目录：`test_artifacts/cad_visual_hierarchy_v055_20260810/`。

## 当前增量：CAD 道路全长覆盖与连续设施匹配（v0.56 验证批次）

- 修复密集道路中心线只取前 64 个源线段的问题。新逻辑按弧长从真实起点到真实终点均匀取样，每条最多 64 个断面；两点中心线也会生成起点、终点两个有效断面，既覆盖全路又保持模型复杂度有上限。
- 同一张真实用户图的 13 条可信道路全部完成全长取样。旧逻辑 13 条均未采样到真实末端，其中 11 条因超过 64 个源线段发生截尾，最大未建模尾段约 190.026 m；新逻辑 13/13 的末端误差不超过 0.000001 m，`full_path_coverage_ratio=1.0`，`source_segments_truncated=0`。
- 斑马线匹配同步改为先投影到完整道路段，再读取最近局部道路轴向；因此稀疏长道路中段可匹配，既有弯道局部切线方向保持不变，并避免同一路相邻断面遮蔽真实路口歧义。
- SketchUp 2026 `26.2.243` 最终代码真机 PASS：266 个顶层对象，含 30 栋六层建筑、13 条全长道路带、196 棵共享树、1 个停车对象、16 个无面复核轮廓和 2 个锁定底图；生成 810 个道路走廊面、95 组车道标线与 62 盏街灯，保存 3,172,338-byte SKP，并成功导出俯视/轴测 PNG。无孤立对象。
- 完整回归 `254/254` 通过。桌面包 EXE 23,097,328 bytes，SHA-256 `30024FA41E546916DC19AACD7287F1B12471FBD120D8238BC81615ED41BDBE32`；688 文件目录包 671,273,951 bytes。启动可响应，复测工作集 182,616,064 bytes、私有内存 105,517,056 bytes；第二实例 code 0 退出，关闭后无残留进程。RBZ 338,500 bytes、12 个成员，SHA-256 `90FDAAEA2DEF6807C3AD8436541990484AF51FF9CF573795B5AD8CA117B7E58A`，已安装插件与测试源码哈希一致。证据目录：`test_artifacts/cad_to_su_full_path_v056_20260810/`。

## 当前增量：建筑层次语义接力与课程模型检查（v0.57 验证批次）

- CAD→SketchUp 会读取建筑图层中明确编码的参数，例如 `BUILDING_RES_6F_FH3.0_FLAT`、`BUILDING_OFFICE_F8_H32_FLAT`、`住宅_6层_层高3.1_平屋顶`。支持 `F6/6F/6层` 楼层、`FH3.0/层高3.0` 标准层高、`H18/高度18` 总高，住宅/办公/商业/学校用途以及平屋顶/双坡/四坡屋顶。只有明确前缀才会解析，普通图层编号不会被猜成高度。
- 参数优先级固定为“逐栋设置 → 明确图层参数 → 全局参数”。图层只有总高而没有楼层时按精确总高生成简单体量，不虚构楼层结构；图层同时有总高和明确/全局楼层时，只做总高除以楼层数的数学换算。逐栋设置可完整覆盖图层参数。
- 结果区新增“建筑图层参数接力”和“课程基础模型检查”。九项检查覆盖建筑轮廓、三维高度、建筑层次、道路、绿化、停车、底图语义接力、候选复核和未交接图元，只报告基础资料完整度，明确不代表课程评分、规范符合或设计质量。
- 真实用户图客观得到 `7/9`：30/30 栋已三维化、37 个道路对象、196 个绿化/树木对象、1 个停车对象、语义底图和零未交接图元通过；“建筑层次”因30栋均为同一18 m住宅且没有逐栋/图层参数而待完善，“候选对象复核”因243个候选待确认而待完善。SketchUp 2026 `26.2.243` 真机继续生成266个顶层对象并保存3,172,202-byte SKP，既有道路和底图位置无回归。
- 独立多高度实例以4个明确图层生成住宅、办公、商业、校园4类用途、3档总高以及平/双坡/四坡屋顶；课程基础模型检查 `8/9`，仅普通 CAD 底图位置需人工复核。SketchUp 2026 真机生成4栋程序化建筑、68条楼层线、344个共享立面组件、8个坡屋顶面、道路/停车/绿化和248,637-byte SKP，无孤立对象。
- 完整回归 `256/256` 通过。桌面包 EXE 23,104,543 bytes，SHA-256 `0FD46E5C135A3D2803A773D71E4D88EBD1873E24887263B95E084FC91D0B324E`；688 文件目录包 671,281,166 bytes。启动可响应，复测工作集 182,857,728 bytes、私有内存 105,988,096 bytes；第二实例 code 0 退出，关闭后无残留进程。RBZ 仍为338,500 bytes且安装插件与测试源码哈希一致。证据目录：`test_artifacts/course_model_semantics_v057_20260810/`。

## 当前增量：工作台内语义引导图编辑与真实 SketchUp 验收（v0.40 验证批次）

- “原图 + 彩色语义引导图”任务页现在提供“新建/编辑引导图”按钮。编辑器在原图之上创建独立的半透明图层；可选择道路、建筑、绿化、水体、停车、橡皮工具，支持左键绘制、滚轮缩放、中键平移和双击适配。它绝不把原图作为绘制目标。
- 保存时强制使用 PNG，拒绝覆盖源图，重新打开并确认与原图像素尺寸完全一致；同时在编辑前后比较原图 SHA-256。只有两项校验均通过，保存路径才会回填至任务页。
- 以用户选定的 1076×1462 底图完成四笔人工道路补画后，结果为 30 个建筑、13 个道路、186 个绿化和 3 个停车闭合 CAD 面；原图 SHA-256 与编辑前一致，引导图尺寸和原图完全对齐。
- SketchUp 2026 `26.2.243` 真机导入 PASS：234 个对象组，包括 30 栋六层建筑、13 个道路、186 个绿化、3 个停车和 2 个锁定底图组；生成可编辑 SKP、顶视 PNG 与轴测 PNG。道路补画只会影响对应语义面，原始线稿仍完整保留为底图，供用户继续人工复核。
- 证据目录：`test_artifacts/full_chain_inapp_editor_v040_20260810/`；运行时报告：`04_sketchup_runtime/sketchup_runtime_report.json`。
- v0.30.0 RBZ：`05_plugin/PlanningToolbox_SketchUp_Importer_v030.rbz`，338,391 bytes，SHA-256 `031020D4BA62A07C64781A321D24609F26121E5978791D05CF88412D29726624`；已同步至 SketchUp 2026 用户插件目录。桌面快捷方式也已重指向 v0.30.0 EXE。

## 当前增量：道路路径绘制与撤销/重做（v0.41 验证批次）

- 语义引导编辑器新增“道路路径”：用户按道路走向逐点点击，系统按同一笔操作绘制连续灰色道路带；双击或“完成路径”提交，避免自由手绘时出现断线、偏移和多次小修改。
- 每条路径只记录轻量的起止点、颜色和画笔宽度；撤销/重做会从初始引导图重放操作，不复制多份大尺寸 QImage，适合 1000 像素以上的总平面图。
- 以同一张 1076×1462 用户底图完成 4 条路径：30 个建筑、13 个道路、186 个绿化、3 个停车闭合面；原图 SHA-256 前后均为 `492fb27390850407e4bb77fae0ab6364dc2a0a4d7ff7b1565903ffcdfde7e0c2`。
- SketchUp 2026 `26.2.243` 实机 PASS：234 个对象组，包含 30 栋建筑、13 个道路、186 个绿化、3 个停车、2 个锁定底图组；无孤立对象，成功保存 SKP 并导出顶视/轴测 PNG。证据目录：`test_artifacts/full_chain_road_path_v041_20260810/`。

## 当前增量：道路小间隙连续性（v0.42 验证批次）

- 语义引导转 CAD 现在只对 `AI_ROAD` 使用半径 2 像素的保守闭合，修复道路节点处由抗锯齿或点击间距产生的小断口；建筑、绿地、水体和停车仍使用半径 1 像素，避免跨对象误合并。
- 人工构造的两段、间隔 2 像素道路在回归中会合并为 1 个道路面；真实 1076×1462 底图仍保持 30 个建筑、13 个道路、186 个绿化和 3 个停车，未出现额外建筑碎片。
- SketchUp 2026 `26.2.243` 实机 PASS：13 个道路对象、30 栋建筑、2 个锁定底图组、0 个孤立对象；生成 17 组车道标线和 6 个方向箭头。证据目录：`test_artifacts/full_chain_road_continuity_v042_20260810/`。

## 当前增量：道路网络质量提示（v0.43 验证批次）

- 语义引导转 CAD 结果新增“道路网络检查”：显示道路面数量、最终网络块数量、小断口修复数量和复核状态。
- 真实底图结果为 13 个道路面、13 个网络块、修复 0 个小断口，系统明确标记为“多个网络块，请检查道路是否断开”；这不是错误修复，而是对当前引导图事实的诚实提示。
- 结果区和帮助页已说明：多个网络块可能是场地内外两组道路，也可能是道路未连通，用户应查看叠加 PNG/CAD 后决定，系统不会擅自猜测道路红线或路口组织。证据目录：`test_artifacts/full_chain_road_network_quality_v043_20260810/`。

## 当前增量：近距离道路断口建议（v0.44 验证批次）

- 结果区新增“近距离断口建议”：只有两个独立道路块之间距离不超过 8 像素且尚未被 2 像素闭合规则连接时，才提示回到“道路路径”工具人工补连；系统不自动改变这两块道路的几何。
- 人工测试图的 5 像素断口被正确报告为 1 条建议；真实用户底图的 13 个网络块之间没有达到建议阈值，因此报告 0 条建议并保持“多个网络块，请检查道路是否断开”。
- 证据目录：`test_artifacts/full_chain_road_gap_review_v044_20260810/`。
- v0.30.0 RBZ：`PlanningToolbox_SketchUp_Importer_v030.rbz`，338,391 bytes，SHA-256 `031020D4BA62A07C64781A321D24609F26121E5978791D05CF88412D29726624`；已同步至 SketchUp 2026 用户插件目录。桌面快捷方式已重指向最新 EXE。

## 当前增量：同像素语义引导草稿与真实 SketchUp 验收（v0.39 验证批次）

- 黑白线稿转换会额外输出与原始图片像素尺寸完全一致的 `*_semantic_guide_template.png`。草稿自动预填高置信建筑、道路、绿化和停车候选；用户可在任意画图软件中只补漏、擦除误判，不需要从零重画。
- 新增“原图 + 彩色语义引导图”模式：原始黑白图继续作为锁定视觉底图，引导图只负责标准色语义分区。系统在任何降采样前要求两图像素尺寸完全一致，并对两份文件执行转换前后 SHA-256 零修改验证。
- 用户选定的 1076×1462 原图真实验证中，自动草稿预填 30 个建筑、12 个道路、189 个绿化和 3 个停车候选；重新转换得到 30 个建筑、12 个道路、187 个绿化及 3 个停车闭合 CAD 面，所有目标图层均为闭合多段线。两棵相邻树在栅格清理中合并，不影响原图底图显示，但仍需人工复核。
- SketchUp 2026 `26.2.243` 真机导入 PASS：234 个对象组，包括 30 栋六层建筑、12 个道路面、187 个绿化面、3 个停车面及 2 个锁定底图组；保存可编辑 SKP，并成功导出 1600×1100 顶视和轴测 PNG。建筑与底图位置一致；道路自动候选仍只覆盖局部走廊，不能冒充完整道路网络。
- 证据目录：`test_artifacts/full_chain_semantic_guide_v039_20260810/`；关键统计见 `semantic_guide_validation_summary.json`，真实 SketchUp 报告见 `04_sketchup_runtime/sketchup_runtime_report.json`。
- v0.29.0 目录版 EXE 已重建：`dist/PlanningToolbox/PlanningToolbox.exe`，23,046,007 bytes，SHA-256 `30CAFB1AE49D0E8993E1FB9EFD4F99ED8B30D036A86A0C30EBBBCE06EF209003`；目录包 671,221,974 bytes / 688 文件，包含 9 个 SKP 组件。
- 桌面快捷方式已刷新并实机启动：正常渲染样本 138.9 MiB RSS / 74.1 MiB Private，进程响应正常；第二实例 8 秒内以 code 0 退出，测试结束后残留进程为 0。
- v0.29.0 RBZ 为 338,392 bytes，SHA-256 `193A88118A80CF7FBF859343276A3C8612D0ADA53F53089E0F0E3A3DAC75B2B8`；SketchUp 2026 用户插件目录已同步为 v0.29.0。

## 当前增量：原图锁定底图、建筑贴线与道路候选（v0.38 验证批次）

- 用户选定 1076×1462 黑白正投影 PNG，SHA-256 `492fb27390850407e4bb77fae0ab6364dc2a0a4d7ff7b1565903ffcdfde7e0c2`；暂按图宽 360 m 校准，图高 489.144981 m、像素 0.334572491 m。尺度仍需用户用已知尺寸确认。
- 重复屋顶改为直接追踪源图闭合边界，不再固定扩成矩形。输出 30 个 `BW_BUILDING_CANDIDATE`；边界密集采样相对原黑线的平均、P90 与最差对象平均偏差均为 0 px。
- 双边线空白走廊通过中轴、宽度稳定性和网络长度筛选，输出 12 个 `BW_ROAD_CANDIDATE` 闭合面。道路边界平均偏差 3.49 px（约 1.168 m），P90 为 12.042 px（约 4.029 m），主要误差来自自动封口与交叉口；必须在语义叠加图中人工删选。
- SketchUp handoff 升级为 schema 7；除 643 个源参考图元聚合成 1 个锁定矢量底图组外，还导入 1 张 SHA-256 校验并锁定的原始 PNG。图片被替换后，交接生成与插件导入都会阻止静默错配。
- SketchUp 2026 `26.2.243` 真机 PASS：30 栋建筑、12 个道路面、189 棵共享树、3 个停车候选、2 个锁定底图组；创建 4,606 个直接面、23,012 条直接边，保存可编辑 SKP 并导出顶视/轴测 PNG。
- 最终证据：`test_artifacts/full_chain_source_aligned_v038_20260810/`，包含源图、DXF、语义叠加 PNG、schema-7 handoff、RBZ、误差 JSON、SKP、运行报告与两张 SketchUp 截图。最终 v0.28.0 RBZ 为 338,392 bytes，SHA-256 `44EC7A4F8FEAA1E8F4EB5AEA0A4DC41FCEFDFAEB45729BFA66976DDB09F7246A`。自动语义仍是学习辅助，不得直接作为审批、施工或法定指标依据。

## 当前增量：复杂住宅图片 → CAD → 修复 → SketchUp（v0.36 验证批次）

- 当前真实输入为 1010×920 黑白住宅总平图，用户明确场地宽度 360 m，每像素约 0.356 m。低分辨率确实限制屋脊、连廊和相邻联排边界，但旧版主要瓶颈同时包括“闭合空地误当建筑”和“树冠辐射线切碎白区”。
- v0.27.0 增加重复屋顶核心、重复坡屋顶面、重叠候选聚合与有界圆形群组识别。真实转换输出 26 个建筑候选、91 个树块和 3 个停车候选；源图 SHA-256 保持不变。
- 质量整理后 2,305 个 DXF 图元进入 schema 6 交接：2,181 个普通参考图元聚合为 1 个锁定 `PT_UNDERLAY`，SketchUp 顶层对象共 121 个，其中建筑 26、绿化 91、停车 3、底图 1。
- 图片来源模型的潜在窗组件为 4,524 个，自适应预算保留 1,872 个（每栋上限 72），不删除建筑、坡屋顶、入口、楼层线或树木。SketchUp 2026 `26.2.243` 真机导入 PASS，创建 26 栋六层坡屋顶建筑、91 棵共享树、26 个入口、104 个屋顶面和 1,872 个窗组件；保存 834,794-byte SKP，并导出两张 1600×1100 PNG。
- 真机导入过程中观察到约 933 MiB 工作集峰值，随后脚本正常保存并退出。该峰值主要来自 SketchUp 视口、18,761 条底图边段及建筑细节，不代表桌面工作台常驻内存；后续仍应继续优化底图显示层级。
- 证据目录：`test_artifacts/full_chain_semantic_v036_20260810/`。当前输出已从“少量体块”提升为课程级完整体量，但图片候选仍必须人工核对楼层、建筑分段和边界，不可直接作为审批或施工模型。

## 历史增量：图片 → CAD → 整理 → 修复 → SketchUp 语义接力（v0.33 验证批次）

- 图片转 CAD 现在会在 DXF 旁生成轻量 `.ptscene.json`。文件只保存来源指纹、比例依据、图层角色、候选对象、复核状态和谱系，不复制完整几何；真实样本经图层整理和质量修复后的 sidecar 分别只有 9,437 bytes 与 9,465 bytes。
- sidecar 与当前 DXF 的 SHA-256 强绑定；DXF 在外部被修改后，过期语义会被中文提示阻断。图层标准化和质量修复会为新 DXF 自动重建 sidecar，并记录父级场景路径与父 DXF 哈希，原始图片/DXF 保持不变。
- SketchUp 交接 schema 升级为 6：已确认/候选建筑、绿化和停车保持独立对象；大量普通描线合并为一个默认锁定的 `PT_UNDERLAY` 参考组。结果区同时显示源几何数、SketchUp 顶层对象数、待复核候选数和底图聚合数；全链路向导显示“语义接力已连接”。
- 真实学生总平面流程通过：源图层整理后 sidecar 校验 PASS；安全修复将 2,469 个 DXF 图元降为 2,228 个（203 组无分叉碎线合并）；最终交接包含 2,223 个源几何，但 SketchUp 顶层对象只有 26 个，其中 1 个锁定底图组承载 2,198 个参考图元、8 个建筑、14 个绿化和 3 个停车候选。顶层对象数量相对逐图元分组减少约 98.8%。
- SketchUp 2026 `26.2.243` 真机验收 PASS：创建 1 个项目根组和 26 个对象组，`PT_UNDERLAY` 数量 1 且确实锁定；保存 850,112-byte 可编辑 SKP，并成功导出 1600×1100 顶视与轴测 PNG。证据位于 `test_artifacts/full_chain_semantic_v033_20260809/`。
- 当前视觉边界：完整线稿作为底图保留，但机器只把 8 个高置信建筑候选自动拉伸；原图中未被识别的建筑仍留在锁定底图中。链接完整性已经解决，下一阶段重点应是提高建筑/道路语义召回率并提供用户确认工作台，不能把候选数量误称为真实总建筑数。
- 新增/更新测试覆盖 sidecar 小体积与过期阻断、修复/标准化连续接力、schema 6 底图聚合、Ruby 锁定导入、结果页初学者提示、向导状态和空闲 GUI 不提前加载 `ezdxf`；全量 `222/222` 通过。

## 当前增量：显式中心线概念道路带（v0.30 验证批次）

- `ROAD_CENTERLINE` / `ROAD_AXIS` / `CENTERLINE` 图层中的 ARC、SPLINE 和开放线默认仍只提供方向辅助；用户在 SketchUp 交接页明确勾选“中心线生成概念道路带（宽度需复核）”后，可输入 4–60 m 的道路总宽度，才生成概念道路带，避免把普通辅助线误识别为道路。
- 概念道路带使用最多 64 个局部切线帧，宽度来源明确记录为 `conceptual_centerline_default`，不会伪装成现场实测或规范道路断面；原始 DXF 继续零修改并保留 SHA-256。
- 新增 GUI 状态保存、后台线程参数传递、结果区统计和 1 项 ARC/SPLINE 交接回归；全量 `215/215` 通过。
- SketchUp 2026 `26.2.243` 真机验收 PASS：以 12 m 总宽度测试，1 条弧形中心线生成 1 个道路带、47 个车行道分段面、94 个人行道分段面、2 条边线、8 段中心标线、4 个方向箭头、4 盏沿局部切线街灯；`PT_CROSSWALK` 使用局部切线自动校正。成果位于 `test_artifacts/sketchup_runtime_v030_centerline_corridor_rerun_20260809_220325/`。
- 中心线验收 RBZ：`output/sketchup_validation_v030_centerline_corridor/PlanningToolbox_SketchUp_Importer.rbz`，337,715 bytes，SHA-256 `71490A99DED65B3969550FD6131A48B78E9E1EF10C4A3B71E5FE823C53E7CD1C`；包内 `main.rb` 98,489 bytes，SHA-256 `8B2F335BE45F9AE7FA926BCBFFE4B6E2E68113A4CAA892113EB3D215D31641FA`。
- 可编辑 SKP 244,478 bytes，SHA-256 `562344B161A2AE7363D3EE85EE3773B8878A65E482BA41E7B37BD29DCB9924C9`；道路细节 PNG 24,156 bytes，SHA-256 `11AFD8B3845D2CB7C4CC164E513C4620D4F0C860D1DEF73B1AE8103651362DE6`。

## 当前增量：曲线采样与环岛道路（v0.29 验证批次）

- ARC、ELLIPSE、SPLINE 进入 SketchUp 交接时使用 `ezdxf.path` 采样，并设置最多 256 个交接点；源 DXF 不变，复杂大圆弧不会因为过密临时点集无限抬高内存峰值。明确命名为 `ROAD_CENTERLINE` / `ROAD_AXIS` / `CENTERLINE` 的曲线可提供局部切线方向，但仍明确标记为概念宽度。
- 明确命名为 `ROUNDABOUT` / `环岛` 的 CIRCLE/近圆曲线才会生成环带道路；环岛使用最多 64 个局部帧，中央岛保持空腔，不把圆形 CAD 线填成实心圆。环岛不生成直行中心虚线，边线和斑马线按环向局部切线处理。
- 新增 2 项交接回归：ARC/SPLINE 中心线采样与上限、命名环岛环带提示；全量 `214/214` 通过。
- SketchUp 2026 `26.2.243` 环岛真机验收 PASS：1 个环岛道路组、24 个主环带面、2 条边线、0 条直行中心虚线；斑马线使用环岛东侧局部切线，方向置信度 `0.879718`。中央空腔在道路细节 PNG 中可见。成果位于 `test_artifacts/sketchup_runtime_v029_roundabout/`。
- 环岛验收 RBZ：`output/sketchup_validation_v029_roundabout/PlanningToolbox_SketchUp_Importer.rbz`，337,595 bytes，SHA-256 `F7EF3A1049F7FB57E82974E6325F132236FDCAD839D089589BDD2A7E85640A8C`；包内 `main.rb` 97,623 bytes，SHA-256 `85893B1958EF0D89D638844066ABAE601E806A60662DD16A94FC7DAF5A008E47`。
- 环岛可编辑 SKP 220,085 bytes，SHA-256 `939633E71AFC7805DFC309D0A474C921C73C5386A4B08FB31517DB1E12FCCF36`；道路细节 PNG 21,045 bytes，SHA-256 `2AC0C19CF8F4DD8198F08EBBC84580C776908258623F4C587027DEC8858DABF8`。

## v0.26.0 新增验证证据

- 规则知识库升级到 `2026.08.5`，新增保守的多段弯道局部切线和显式道路中心线辅助。闭合弯道只有在两侧边界近似平行、宽度稳定时才生成最多 64 个局部帧；`ROAD_CENTERLINE` / `ROAD_AXIS` / `CENTERLINE` 开放线只用于方向辅助，概念宽度会显式标记。当前工作树进一步把可信弯道局部帧接入 SketchUp 插件，沿帧序列生成分段人行道、道路边线、中心虚线和方向箭头；不可信路段仍回退为原始 CAD 面。
- 新增 2 项端到端交接测试：弯道局部切线、明确道路中心线；同时修复接近水平负零角度跳转到 179.99° 的稳定性问题。全量 `212/212` 通过，专项 `30/30` 通过。
- v0.26.0 RBZ 为 333,684 bytes，SHA-256 `EE1CD0BC4507638421861520C3A1684F4C2B4BF9214CAC44FF942A459B811F2B`；包含 9 个 SKP、无 GLB/SKB，包内 `main.rb` SHA-256 为 `AE9FBBFFD6931F5E60D185E9DAF68E6427580EB2F46C167625839C1696EF06C9`。
- 当前弯道细化工作树重新生成的 RBZ 位于 `output/sketchup_validation_v027_curved/PlanningToolbox_SketchUp_Importer.rbz`，大小 336,127 bytes，SHA-256 `4565314772BA6F435A8F77FB0AE2AF59A1D57CDCBF66CF6C0905F74230C29020`；包内 9 个 SKP，且已确认包含 `curved_road_records`、`curved_road_sample` 和 `add_curved_road_details`。
- 当前工作树重新打包的目录版 EXE 已完成：`dist/PlanningToolbox/PlanningToolbox.exe`，23,008,471 bytes，SHA-256 `CA846B2CB659B7842BB0DF684F6A666CAB6073F355F09B3BFDBF5B946E472D8D`；隐藏启动保持响应，约 139.1 MiB RSS / 73.5 MiB Private，第二实例在 3 秒内以 code 0 退出。包内 SketchUp `main.rb` 已包含环岛环带实现，SHA-256 `85893B1958EF0D89D638844066ABAE601E806A60662DD16A94FC7DAF5A008E47`。
- SketchUp 2026 `26.2.243` 当前弯道真机验收 PASS：1 条弯道道路真实生成 1 个局部道路细化面、6 个分段人行道面（两侧 2 条带）、2 条道路边线、4 段中心虚线、2 个方向箭头；`PT_CROSSWALK` 使用弯道局部切线，斑马线长条轴与道路轴均为 `12.123°`，方向置信度 `0.682936`。可编辑 SKP、全景 PNG、道路细节 PNG 和运行报告位于 `test_artifacts/sketchup_runtime_v027_curved/`。
- v0.26.0 后续弯道设施升级：弯道街灯沿局部切线在道路两侧布置，组件记录道路几何元数据，并继续使用过街避让区。新版 RBZ 位于 `output/sketchup_validation_v028_curved_lights/PlanningToolbox_SketchUp_Importer.rbz`，大小 336,604 bytes，SHA-256 `5D269A9D6427A79706422224AA98B0AF59C40D8175E883B6710ED8AE89436C49`；源码/EXE 内 `main.rb` SHA-256 为 `13F0647824ADE329DFD70C4DA94DCDE3C5EC6031E516CB341839F72F09337913`。
- SketchUp 2026 `26.2.243` 弯道街灯真机验收 PASS：1 个弯道局部道路面、6 个分段人行道面、2 条边线、4 段中心虚线、2 个方向箭头、2 个沿局部切线街灯；2 个冲突街灯候选被过街区避让。每个生成街灯均写入 `curved_local_tangent`、局部站点、方向角和道路侧别元数据。成果位于 `test_artifacts/sketchup_runtime_v028_curved_lights/`。
- 两个已知桌面快捷方式仍指向同一 v0.26.0 EXE、项目工作目录和原有项目图标，只更新了说明文字。

## v0.25.0 历史验证证据

- 规则知识库升级到 `2026.08.4`（16,932 bytes、10 项可追溯来源），增加斑马线、交通灯和道路设施安全规则。普通 `PT_CROSSWALK` 自动匹配最近的可信近矩形道路；水平、垂直和 30° 道路均有旋转断言。`PT_CROSSWALK_FIXED` / `PT_CROSSWALK_MANUAL` 保留 CAD 角度；十字路口歧义、异形道路、未匹配和低可信结果安全回退，不伪造道路方向。
- 匹配后的斑马线按车行道宽度设置横跨距离，并为中心虚线、方向箭头和自动街灯写入过街避让区。真机场景中主动取消 2 段中心虚线和 2 盏冲突街灯；道路仍保留 24 段中心虚线、4 个箭头和 16 盏共享街灯。
- 七条白色标线由插件生成 7 个独立薄实体（42 个原生面），原 KayKit 道路块仅保留来源、许可证和尺寸审计，避免隐藏网格或道路底板与宿主道路冲突。交通灯继续使用明确 CAD 角度，系统不自动推断信号控制。
- SketchUp 2026 `26.2.243` 真机验收 PASS：29 个对象、4 栋建筑、2 条道路细化面、2 条人行带、4 条边线、1 个自动匹配斑马线和 2 个交通灯。水平道路轴与斑马线长条轴均为 0°；过街宽度 4.0 m、横跨车行道 9.3 m、厚度 0.08 m；31 个组件实例复用 8 个定义。SKP（526,038 bytes）、全景 PNG、道路细节 PNG 与运行报告位于 `output/sketchup_validation_v025_final10/`。
- 最终 RBZ 为 333,666 bytes，SHA-256 `E3D4130F8898CA22C44922E41A0B5854095DED80FCC0CDE04C89798C4582D53E`；包含 9 个 SKP，不包含 GLB/SKB，包内 `main.rb` 与源码 SHA-256 均为 `847D2B6368357A688C04B9F0AF8332B7F6B420FB010137EB54C6BFD2AA858EEC`。
- 两个已知桌面快捷方式均指向 v0.25.0 EXE、项目工作目录和既有项目图标；OneDrive 桌面快捷方式真实启动 PASS。规则和方向语义参考国家标准信息公共服务平台的 `GB 5768.3-2025` 元数据与 FHWA MUTCD 11th Edition Part 3，但结果仍明确为教学建模辅助，不是规范合规证明。

## v0.24.0 历史验证证据

- 道路细化增加 `跟随模型精度 / 完整街道 / 基础车行道 / 关闭` 四档用户选择。接近矩形的闭合 ROAD 面可生成双侧人行带、开放端部路缘、两条边线、中心虚线、双向箭头和共享街灯；不规则道路保留原 CAD 轮廓，每条道路的人行道、箭头、标线和设施均有硬预算。
- 规则知识库升级到 `2026.08.3`（14,135 bytes、8 项可追溯参考）。实现参考 Godot Road Generator 的横断面/分段预算和 Streetmix 的街道组成交互，但未复制第三方代码；Streetmix 的 AGPL 代码、OSMnx/Lanelet2 的重运行时和 Truevision Designer 的自定义 EULA 内容均未引入。
- 组件目录升级到 `2026.08.3`：新增 KayKit City Builder Bits 的斑马线道路块和交通灯两件 CC0 资产。9 个内置原生 SKP 共 342,348 bytes，低于 500,000 bytes 硬预算；CAD 中 `PT_CROSSWALK` / `PT_TRAFFIC_LIGHT` 显式块触发组件并保留旋转角，不自动猜测信号控制路口。
- SketchUp 2026 `26.2.243` 首轮视觉验收暴露组件包围盒 X/Y/Z 轴顺序错误：斑马线被拉成长条、街灯/树木/信号灯过高。修复后真机尺寸为斑马线 8.0×8.0×0.12 m、信号灯 0.9×0.8×5.0 m、道路含街灯高度约 6.16 m；回归断言锁定 `width/height/depth` 顺序。
- 最终真机验收 PASS：29 个对象、4 栋建筑、2 条道路细化面、2 条人行带、4 条边线、26 段中心虚线、4 个方向箭头、18 盏共享街灯、1 个斑马线组件和 2 个信号灯。方向箭头分布在道路约 1/3 与 2/3 位置，避开常见中心过街区；斑马线改用低饱和灰色底面和白色条纹，隐藏外部组件的纹理网格边。33 个组件实例只加载 8 个定义；SKP（523,582 bytes）、全景 PNG、道路细节 PNG 与运行报告均成功写出，成果位于 `output/sketchup_validation_v024_final2/`。
- 最终 RBZ 为 332,795 bytes，SHA-256 `1D57179B1FB843143363D19A759BB1EC14B6A93B40ECE89383A1929F7ED8D3D0`；包含 9 个 SKP，不包含 GLB/SKB。两个桌面快捷方式均指向 v0.24.0 EXE、项目工作目录和项目图标，OneDrive 桌面快捷方式真实启动 PASS。

## v0.23.0 历史验证证据

- 新增 `sketchup_component_catalog.json` 与严格加载器：6 个许可清晰的来源、7 个内置 CC0 原生 SKP、193,334 bytes 总量、500,000 bytes 硬预算；逐件校验文件名、来源、许可证、原始/目标尺寸、实例预算、文件大小和 SHA-256。
- CAD→SU 现在优先复用组件：树木按树冠尺度选择大/小树，建筑类型选择雨棚/挑檐，汇报道路沿长边按 18 m 间距生成最多 12 盏街灯；`PT_PLANTER` 与 `PT_PARASOL` 显式块调用花池与遮阳伞。树和入口组件缺失时安全回退，显式组件失败时保留原块线稿。
- SketchUp 插件使用懒加载共享定义，记录组件编号、来源、许可证及 bundled/user override 来源；扩展程序菜单可打开自定义组件文件夹，用户以固定文件名替换轻量 SKP，不需修改代码或增大主安装包。
- 专项与 GUI 回归 `53/53` 通过；完整回归 `203/203` 通过。空闲窗口测试确认本次组件信息展示不会提前加载 ezdxf、Shapely、Matplotlib、SciPy、scikit-image、openpyxl 或 reportlab。
- SketchUp 2026 `26.2.243` 真机验收 PASS：25 个顶层对象、4 栋建筑、463 个共享窗、72 条楼层线、4 个 CC0 入口构件、8 盏共享街灯、6 棵共享树、1 个花池和 1 把遮阳伞。20 个组件实例只加载 6 个定义；最终 SKP 410,755 bytes，较 v0.22 样本增加 94,463 bytes；原生 SKP 保存与 1600×1000 PNG 输出均为 true。成果位于 `output/sketchup_validation_v023_final/`。
- 最终 RBZ 为 193,157 bytes，SHA-256 `8A89CC054022C5C7A3323E967BC4F4EFD71793E0DA05A7DD4A2A751B4DB72C36`；桌面两个已知快捷方式均指向 v0.23.0 EXE、项目工作目录和原项目图标，OneDrive 桌面快捷方式实际启动 PASS，测试进程随后关闭。

## v0.22.0 新增验证证据

- 新增 `sketchup_modeling_rules.json` 与严格校验加载器。磁盘规则文件为 10,743 bytes，记录 6 项来源、3 档细节、5 类建筑、4 类场地和 2 档植被规则；自动测试强制禁止嵌入 PNG、SKP、Base64、模型权重和未审计代码复制。
- 建筑立面模数、窗尺寸、入口、阳台资格、低饱和颜色、立面预算，场地高差/薄层/边缘/道路标线，以及树冠尺度、分段数、组件复用与变化幅度均从知识库读取，不再是散落的硬编码表。
- `.ptsu.json` 记录知识库 ID、版本、来源数、非规范性边界和“用户设置优先”声明；SketchUp 项目根组保存知识库 ID 与版本，便于后续审计和增量更新。
- 知识库来源矩阵覆盖 OGC CityGML 3.0、CityJSON（CC0-1.0）、OSM2World（MIT）、SketchUp 官方 Ruby API 教程（MIT）、SketchUp 官方实体说明和 3D City Database（Apache-2.0）。当前版本只参考结构/算法思想，没有复制第三方业务代码。
- 专项、界面与内存兼容测试 `61/61` 通过；完整回归 `200/200` 通过。PyInstaller 包内规则文件存在，未新增第三方依赖。
- SketchUp 2026 `26.2.243` 真机验收 PASS：保存 1 个项目根组、23 个顶层对象、4 栋建筑、463 个共享窗、72 条楼层线、4 组入口/雨棚、3 个阳台、1 个屋顶设备、6 棵共享树（1 个树定义）、10 个场地分层面和 10 个道路中心虚线；SKP 保存与 1600×1000 PNG 输出均为 true。成果位于 `output/sketchup_validation_v022/`。
- 桌面两个已知快捷方式均已更新为 v0.22.0 EXE、项目工作目录和项目图标；OneDrive 桌面快捷方式实际启动后进程正常响应，测试进程随后关闭。

## v0.21.0 新增验证证据

- schema 5 增加建筑基座、入口与雨棚；汇报住宅增加最多 8 个共享阳台，平屋顶增加轻量设备箱，并在入口位置避让首层窗。
- 道路/停车/水体/绿地增加路缘、标线、水岸或收边，规则矩形道路生成有界中心虚线；建筑类型使用不同低饱和材质，树木提供确定性旋转与 0.92–1.08 缩放变化。
- 完整回归 `197/197` 与 SketchUp 2026 真机验收通过，插件继续接受 schema 1–5。

## v0.20.0 新增验证证据

- `.ptsu.json` 升级为 schema 4，SketchUp 插件继续接受 schema 1、2、3、4。课程/汇报模式为闭合道路、绿地、水体和停车轮廓写入克制的视觉高差与薄层；快速体量模式不产生额外场地细节。
- `PT_TREE`、`TREE_SYMBOL` 等叶级树块会按 CAD 符号尺度生成分层圆冠的低多边形树。尺寸量化后复用 SketchUp 组件定义，嵌套 `TREE_CLUSTER` 只转换内层树符号，不重复生成整组。
- SketchUp 2026 `26.2.243` 真机验收创建 23 个顶层对象、4 栋程序化建筑、468 个共享窗实例、72 条楼层线、6 个三维树实例、1 个共享树定义、10 个场地分层面和 9 个薄层；SKP 保存与 1600×1000 PNG 输出均为 true。
- 真机报告纠正包围盒轴向后，四栋建筑 Z 向高度分别为 13.10 m、38.34 m、10.10 m、23.94 m，与楼层、层高及屋顶/女儿墙参数一致。成果位于 `output/sketchup_validation_v020/`。
- 结果页与帮助页新增树组件、场地分层、插件更新和视觉高差边界说明。树和窗均共享定义；未新增 Python 依赖，不打包 SketchUp，不需要 API。
- 新增生成 DXF 的“用作下一步工作图”续接和轻量谱系记录；图片/GIS/DWG 转换自动续接，图层标准化、质量修复和方案草图需用户确认，原始文件保持不变。
- CAD→SU 专项 `14/14`、工作流/项目/GUI 联合 `55/55`、完整回归 `196/196` 通过。测试产生的 `output/gui_test` 文件已清空。
- v0.20.0 EXE 隐藏启动保持响应，单实例保护通过；138.3 MiB RSS / 73.2 MiB Private，目录包约 639.6 MiB，未因三维树和场地表达引入大型运行库。

## v0.19.0 新增验证证据

- 新增顶部“🧩 流程”入口和九阶段全链路向导：项目设置、资料导入、无损预检查、图层标准化、质量修复、规划分析、GIS 交换、SketchUp 交接和成果导出。
- 向导只保存轻量 JSON 状态并复用现有任务页面。项目配置、有效 DXF 和已知单位由现有证据自动确认；任务成功、GIS/图片转 DXF、SketchUp 交接及成果导出均通过回调更新进度，不把简单浏览误判为完成。
- 图层标准化、GIS 和 SketchUp 被明确标注为可选；质量检查、单位预检查和规划分析不能通过“跳过”绕过。AI 图片或 GIS 导入生成 DXF 后自动载入新文件并重新执行后台预检查。
- `.ptx` 项目保存资料来源、当前步骤、完成步骤和明确跳过步骤；旧项目缺少该字段时自动使用安全默认值。向导内可直接保存，关闭后继续。
- 960 像素窄屏时顶部按钮自动收缩为带可访问名称和提示的图标模式；向导阶段列表禁用横向滚动并支持自动换行，避免新增入口造成界面溢出。
- 新增 4 项工作流状态/界面回归；完整回归 `191/191` 通过。最终 EXE 隐藏启动保持响应，单实例保护通过，空闲样本与 v0.18.0 基本持平，目录包仍为 639.6 MiB。

## v0.18.0 新增验证证据

- 新增“逐栋设置高度与模型样式”窗口：从当前建筑图层只读列出顶层闭合轮廓，可多选后批量设置楼层、层高、建筑类型、屋顶和精度；未设置建筑继续使用全局参数。
- 每栋建筑使用项目 UUID + DXF handle 形成稳定身份；逐栋设置随 `.ptx` 保存和恢复。项目身份变化时可按来源 handle 重新匹配；CAD 删除或重画轮廓造成的失配会在结果区显示请求数、成功匹配数和未匹配数。
- 双建筑差异测试验证：同一 DXF 中一栋使用全局 3 层 × 3.0 m 生成 9.0 m 体量，另一栋使用逐栋 10 层 × 3.6 m 生成 36.0 m 办公建筑、双坡屋顶和汇报精度；两者指纹和建模参数独立，源 DXF SHA-256 不变。
- 逐栋参数缺少正层高时强制阻断；界面参数可传入后台线程，且保存项目后可恢复。逐栋列表扫描不展开曲线点集，避免仅为了显示建筑列表而制造大临时几何。
- 750 栋压力用户流全部交接，30 栋使用独立汇报参数，立面实例仍受 16,000 上限控制；Python 跟踪峰值低于测试警戒线 128 MiB。压力 DXF、JSON 和目录均由测试上下文自动删除，无残留。
- SketchUp 专项测试由 9 项增至 13 项；完整回归 `187/187` 通过。覆盖稳定身份、独立高度、参数阻断、GUI 表格、项目保存、750 栋压力、源文件零破坏、旧 schema/RBZ 兼容。
- v0.18.0 EXE 为 22,933,327 bytes，SHA-256 `BD77A4E6CB2BD5623A7F5D24426CDEEC51ADEDA024A484530B1B3A3272502E04`，目录包 639.6 MiB；隐藏启动保持响应，第二实例自动退出。桌面 `Planning Toolbox.lnk` 已重新创建并实际启动验证，目标、工作目录和自定义图标正确。
- SketchUp 2026 `26.2.243` 已完成真实运行验收。首次导入暴露 `Sketchup::Entities` 在 2026 版不支持 `empty?` 的兼容缺陷；代码改用原生 `length`，增加回归断言，重建 RBZ 并重新安装后通过。
- 修复后真实导入创建 1 个项目根分组、16 个对象和 4 栋程序化建筑；生成 468 个共享窗组件、8 个屋顶面、8 个女儿墙面、72 条楼层辅助线和 8 个 `PT_*` 标签。SketchUp 报告 `model_save_status=true`、`image_write_status=true`。
- 真实成果位于 `output/sketchup_validation_v018/`：`planning_toolbox_su_validation.skp`、1600×1000 原生视口图 `planning_toolbox_su_validation.png`、`sketchup_runtime_report.json`、交接文件与新版 RBZ。结构与运行验收通过，但建筑造型、材质和规划表达质量仍需按具体作业要求人工审美复核。

## v0.17.0 新增验证证据

- `.ptsu.json` 升级为向后兼容的 schema 3；插件接受 schema 1、2、3。每个对象增加确定性 SHA-256 几何指纹，同一项目可识别未变化、已变化和已删除但安全保留的顶层对象。
- 新增“课程作业 / 快速体量 / 汇报模型”三档预设，以及居住、办公、商业、校园、通用建筑类型；四边形建筑可生成双坡或四坡屋顶，平屋顶生成女儿墙，所有三维建筑可生成楼层辅助线和共享窗组件。
- 新增增量导入：未变化对象不重建、变化对象自动替换；SketchUp 扩展程序菜单可锁定/解除锁定选中对象，锁定后保留手工精修结果。
- 大型场地采用均匀分配的立面细节预算：课程模式最多 8,000 个、汇报模式最多 16,000 个共享窗实例；不会删除建筑、楼层线或屋顶。200 栋 × 6 层压力用户流的潜在 26,400 个窗被均匀控制为 8,000 个。
- 200 栋课程模型压力交接：201 个对象、200 个程序化建筑、4,000 条楼层辅助线元数据，耗时 0.1971 s，Python 跟踪峰值 1.969 MiB，交接文件 300.3 KiB，原 DXF SHA-256 不变；临时测试目录已自动删除。
- 9 项 SketchUp 专项测试与完整回归 `183/183` 通过；覆盖模型预设、指纹稳定性、schema 兼容、RBZ 原生组件调用、后台参数传递、界面默认值和零破坏保护。
- v0.17.0 EXE 为 22,916,182 bytes，SHA-256 `E5B28CEE56AB72CAADEB42536490A2391EAB491A5E2309282DBDCF5624FB9862`，目录包 639.6 MiB；离屏隐藏启动 10 秒为 82.1 MiB RSS / 49.2 MiB private，第二实例自动退出，测试进程已清理。桌面快捷方式目标与自定义图标均有效。
- 当前测试机未安装 SketchUp/Ruby，因此 RBZ 的真实安装、视口材质/屋顶朝向、组件显示和最终 `.skp` 保存仍是 **PENDING USER VALIDATION**；不能把结构测试等同于 SketchUp 端最终视觉验收。

## v0.16.0 新增验证证据

- `.ptsu.json` 升级为向后兼容的 schema 2；SketchUp 插件同时接受 schema 1 和 2，旧版交接文件不会被强制淘汰。
- 新增 `INSERT` 与嵌套 `INSERT` 递归交接，保留块名称、父子分组、稳定 `PT-BLOCK-*` ID，并让块内 0 图层对象继承插入层。
- 新增 `3DFACE`、`SOLID`、`TRACE` 面交接，以及默认关闭、可由用户明确开启的 `TEXT`、`MTEXT`、`ATTRIB`、`ATTDEF` 文字交接。
- 界面、后台线程、结果统计、作业助手和完整帮助页已同步三个兼容开关；默认开启图块与三维面、关闭文字，以控制模型杂乱度和内存。
- 新增 2 项复杂 CAD 自动化场景；完整回归 `182/182` 通过，原有 180 项无回退。
- 压力用户流：120 个二级图块实例、10 个三维面、20 条模型空间文字，共交接 391 个几何对象、240 个块分组和 140 条文字；耗时 0.4552 s，Python 跟踪峰值 4.954 MiB，1,050.0 KiB 交接文件，源 DXF SHA-256 不变，临时目录已自动清理。
- v0.16.0 EXE 为 22,909,771 bytes，SHA-256 `B9D55FFB691D29FC12D5D770CB63D94834DCD9752A2D9650DBE7E9522166A164`，目录包 639.6 MiB；离屏隐藏启动 10 秒为 81.2 MiB RSS / 48.4 MiB private，和 v0.15.0 同口径常驻内存持平。

## v0.15.0 新增验证证据

- 第 10 个桌面任务“CAD → SketchUp 模型交接”已接入 QThread 后台执行、项目保存恢复、结果文件列表、中文作业助手和帮助页。
- `.ptsu.json` 保存米制局部坐标、可逆项目原点、稳定 `PT-*` 对象 ID、原 DXF handle、来源图层、对象角色及明确的建筑拉伸高度。
- 投影项目未启用近原点、经纬度坐标、`EPSG:3857`、`$INSUNITS=0`、三维模式未填写层高均会阻断；二维模式必须明确使用楼层 0。
- RBZ 遵循官方扩展结构：根加载器 `planning_toolbox_sketchup.rb` + 同名支持目录；插件通过 SketchUp 主线程事务创建分组、标签、面和推拉体，并写入对象属性字典。
- 真实样例用户流程：11 个 DXF 对象全部交接，4 个建筑按 6 层 × 3 m 写入拉伸参数；交接 JSON 8,467 bytes，RBZ 3,264 bytes，耗时约 0.042 s，Python 跟踪峰值约 1.272 MiB，源 DXF SHA-256 不变，临时目录已自动清理。
- 新增 6 项自动化测试，覆盖近原点、稳定 ID、二维/三维模式、单位/层高阻断、RBZ 结构、TaskWorker 双文件输出及第 10 个 GUI 页；完整回归 `180/180` 通过。
- 当前测试机未安装 SketchUp/Ruby，因此 RBZ 的真实安装、SketchUp 视口显示和最终 `.skp` 保存仍明确标记为待用户在安装 SketchUp 后人工验证；不得据此宣称 SketchUp 端最终 STABLE。
- PyInstaller v0.15.0 目录包构建成功：EXE 22,904,323 bytes，SHA-256 `8C411A820759B8469C98E5BC9C546FE844F19695491A499D605F8F9238A036B0`，整包 639.5 MiB；RBZ 的 3 个资源文件均已进入 `_internal`。隐藏启动 10 秒后 81.2 MiB RSS / 48.6 MiB private，界面保持响应，第二次双击实例自动退出。

## v0.14.0 新增验证证据

- 自动适配顺序升级为 `ArcGIS Pro → QGIS/GDAL → 内置 GeoJSON`。本机已检测到 ArcGIS Pro 3.7、ArcPy `ArcInfo` 许可、JSONToFeatures、FeaturesToJSON、Project 和 ExportCAD。
- Planning Toolbox 主进程不导入 ArcPy；扩展任务使用 ArcGIS Pro 自带 `arcgispro-py3/python.exe` 独立运行 5.7 KB 调度脚本，不打开 ArcGIS Pro 图形界面、不复制或重新分发 Esri 运行时。
- 真实互操作流程一：EPSG:4547 GeoJSON → 122,880-byte GPKG → EPSG:4547 GeoJSON → DXF。1个面完整往返，最大 X 坐标保持 500100.0，DXF `$INSUNITS=6`、审计0错误、源哈希不变。
- 真实用户后台流程二：`sample_parcels.dxf` 通过 TaskWorker 导出 GPKG，再通过 TaskWorker 导回 DXF；两向适配器均为 ArcGIS Pro，导回3个面，DXF审计0错误、原样例哈希不变。
- GeoPackage 多面图层不擅自选择：只有一个面图层时自动使用；多个面图层时明确列出并要求用户先在 ArcGIS Pro 中导出目标图层。
- ArcGIS Project 使用项目 EPSG；若需要地理转换，使用 ArcGIS 列出的首选转换并把转换名称写入元数据和结果表，便于人工复核。
- 新增5项自动化测试，覆盖 ArcGIS 安装检测、缺失提示、无 shell 参数列表、ArcGIS 优先级和 OGR 回退。完整回归 `174/174` 通过。
- 当前 QGIS/GDAL 外部驱动真实互操作仍未执行，但本机 ArcGIS Pro 路径已经真实验证；因此用户无需再安装 QGIS。

## v0.13.0 新增验证证据

- GIS 页由2种模式扩展为4种：基础 GeoJSON↔DXF 保持兼容；新增 GPKG/SHP→DXF 与 DXF→GPKG。扩展模式必须使用 `.ptx` 中经确认的米制投影 EPSG。
- 可选桥接只检测 PATH、QGIS、OSGeo4W 的明确系统安装位置，不扫描用户目录，不把 QGIS/GDAL 打包进 EXE。缺失时返回中文安装提示，现有功能不受影响。
- 坐标安全新增三层阻断：经纬度、EPSG:3857、无明确 EPSG/非米制/非投影项目坐标均不得进入扩展转换；可选 pyproj 仅在源码环境安装后按需加强 EPSG 复核。
- DXF→GPKG 先根据真实 `$INSUNITS` 把毫米、厘米、英尺等坐标换算为米；GIS→DXF 再按用户选择的目标单位换算。专项往返测试验证 1 m 恢复为 1000 mm，且 `$INSUNITS=4`。
- GPKG/SHP 输入先在自动清理的临时目录中转为项目投影 GeoJSON，再调用原有 Polygon/MultiPolygon→LWPOLYLINE 写入器；中间文件不残留。
- GIS/CAD 源文件在转换前后执行 SHA-256 校验；外部命令使用参数列表与 `shell=False`，禁止把输入路径作为输出路径。
- 修复现有 GUI GeoJSON 回导的参数名缺陷；回归测试真实生成 `$INSUNITS=6` 的 DXF 并重新读取成功。
- 新增9项测试，覆盖 CRS 阻断、适配器检测、命令参数、GPKG/GeoJSON 源文件只读、坐标单位往返、GUI 四模式和后台回导。完整回归 `169/169` 通过。
- 当前测试机未安装 QGIS/GDAL；该第二适配器仍使用可控替身验证命令和安全逻辑。ArcGIS Pro 3.7 第一适配器已经完成真实 GPKG/DXF 互操作测试，不再属于待验证状态。

## v0.12.0 新增验证证据

- `.ptx` 项目格式升级为向后兼容的第二版，保存项目 UUID、名称、类型、项目 CRS、CAD 单位、SketchUp 近原点变换和稳定 `PT-*` 对象编号。
- 本地建模原点 X/Y/Z 与旋转转换具备可逆测试；旧 `.ptx` 根据路径生成稳定兼容身份，不修改旧项目文件。
- 项目设置保持纯元数据和按需加载，不打包 GIS 数据库或三维引擎。

## v0.11.0 新增验证证据

- 新增3套可选择的中国规划制图辅助模板：课程总平面、居住区总平面、国土空间规划图件。模板按项目类型装入18–25个相关图层，不把全部40类图层无差别塞进每张图。
- 新增40类轻量图层数据，覆盖控制边界、道路交通、建筑、景观生态、自然资源、公共设施、标注和图幅要素；既有基础8图层配置和旧命令仍保持可用。
- 标准来源注册表记录标准号、名称、主管部门、状态、实施日期、适用范围和官方核对入口。当前索引包含7项依据，明确区分现行、试行和“使用前复核现行状态”。
- 图层标准化现在会在输出副本上同时统一已有标准图层的颜色、线宽和线型；原始 DXF 继续通过 SHA-256 校验保持不变。
- 新增中国制图辅助检查器：未知单位列为阻断项；缺失/空的必备图层、未识别图层和无法从 DXF 可靠判定的地理坐标系列为待人工确认；输出中文 TXT 与 JSON。结论固定声明为辅助一致性，不代表法定审查或审批通过。
- 新生成的空白模板包含5个可编辑矢量块定义：`PT_NORTH_ARROW`、`PT_SCALE_BAR_100M`、`PT_ENTRANCE`、`PT_TREE` 和 `PT_PARKING_STALL`，不自动插入模型空间，不制造虚假图面内容。
- GUI 默认启用“中国规划课程总平面”模板，并可关闭或切换；选择状态随 `.ptx` 项目保存恢复。CLI 新增 `--drafting-profile`，模板生成和图层标准化均可使用。
- 新增5项自动化测试，覆盖配置筛选与来源追溯、图层样式校正、未知单位阻断、GUI后台输出、CLI模板/标准化流程。完整回归 `153/153` 通过。
- 打包归档确认包含 `planning_toolbox.rules.drafting`、`planning_toolbox.cad.layers.compliance`、新版图层管理器和 `china_drafting_layers.yaml`。隐藏启动内存较 v0.10.0 的 136.3 MB 增加约 0.8 MB，符合轻量按需加载目标。

## v0.10.0 新增验证证据

- 知识库不再只是被动记录：人工精修并明确标记为 `user_curated` 的 DXF 会提取米制建筑长宽、停车位长宽和树木符号半径，形成轻量质量画像；不保存完整图元到内存。
- 黑白图转 CAD 会在运行前匹配同一输出知识库、同一图纸类型的质量画像。建筑、停车位和树木只有在识别尺寸接近精选样本时才吸附到人工精修尺寸；建筑最大允许相对差异 25%，停车位 32%，树木半径 30%，超出范围保持原样。
- 知识辅助对照测试中，精修样本为建筑 `20 × 10 m`、停车位 `5 × 2.5 m` 和树木半径 `1.5 m`。近似栅格结果实际触发建筑及重复图元校正；重新读取输出 DXF，6 个停车位块的 `xscale=5.0`、`yscale=2.5`。
- 普通规则下两个封闭停车位不会提升（0 个）；加入同类型、人工确认的 `5 × 2.5 m` 样本后识别为 2 个，并输出两个标准尺寸 `PT_PARKING_STALL` 块。该放宽仍要求至少两个相似对象，单一矩形不会因知识样本自动提升。
- `candidate_unreviewed` CAD 永不参与几何校正；图纸类型为“待确认”时也禁用知识辅助，避免跨项目类型污染。单位未知的精选 CAD 只保留来源记录，不生成米制尺寸画像。
- GUI 默认开启“使用已确认的精修 CAD 知识辅助校正”，结果表显示匹配精修 CAD 数量、实际校正图元数量；无可用样本时完全回退原算法。
- 完整回归 `148/148` 通过。打包归档确认同时包含知识画像模块与图转 CAD 模块；启动内存仍为 136.3 MB，与 v0.9.0 相同，不需要 API 或常驻模型。

## v0.9.0 新增验证证据

- 图转 CAD 后默认生成轻量 Markdown 知识卡；卡片只记录路径、SHA-256、明确比例依据、识别摘要、成果路径和复核状态，不嵌入原图、缩略图、像素矩阵或模型权重。
- 相同原图与相同转换配置使用确定性卡片 ID，重复运行更新同一张卡；不同场地宽度等关键配置产生独立卡片。既有人工复核状态与备注在更新时保留。
- 识别可信度未通过真值集校准时明确写为 `not_calibrated`，机器对象写为候选，不生成虚假的百分比，也不允许仅凭 Markdown 进行面积、退线或规范结论。
- GUI 图转 CAD 后台任务在处理前后对原图执行 SHA-256 校验；知识卡测试验证源文件不变，指纹不一致时拒绝建卡。
- CAD 采用主动精选策略：默认不创建 `cad_samples` 目录；用户明确收藏时才复制一份 DXF。复制前后校验源 DXF，记录 `$INSUNITS`、图层、模型空间图元数、来源路径与 SHA-256。
- 结果区新增“⭐ 收藏精修 CAD”，用于把人工检查后的 DXF 标为 `user_curated`；自动输出只能标为 `candidate_unreviewed`，不会冒充精修标准样本。
- 独立进程测试确认：只导入/检索知识卡模块不会加载 PIL、NumPy、scikit-image、ezdxf 或 Shapely；图像和 CAD 引擎只在用户实际转换或收藏 CAD 时按需加载。
- 完整回归 `144/144` 通过；打包归档确认包含 `planning_toolbox.knowledge.image_cards`。桌面快捷方式目标存在并指向新版目录 EXE。

## v0.8.4 新增验证证据

- 黑白线稿增加底色策略：自动判断、白底黑线、黑底白线。自动策略使用图片边缘的主要底色做可解释判断，报告记录请求策略、实际判断和边缘亮度；用户也可在界面手动覆盖。
- 建筑候选支持旋转矩形。旧版约 28% 图面跨度限制放宽到 38%，同时使用旋转矩形贴合度与复杂度联合约束；真实同图复测中，右侧复杂停车区没有被误判为第 9 栋建筑，仍正确输出 8 栋。
- 至少三个尺寸与方向相近的封闭窄矩形可归并为一个 `PT_PARKING_STALL` 块定义和多个 `INSERT`，输出至 `BW_PARKING_CANDIDATE`；单个小矩形不会自动提升为停车位。
- 规则圆形/椭圆候选的最小尺度由图面面积 2% 放宽到 0.3%，可覆盖中型环岛、广场或景观轮廓；仍需满足紧致度与椭圆形态约束。
- 综合黑底白线场景（900×600、明确宽度 180 m）正确识别 2 个旋转建筑、4 个树木块、6 个停车位块和 1 个 ELLIPSE；顶点由 1,328 降至 945，减少 28.8%，DXF 审计 0 errors / 0 fixes，`$INSUNITS=6`，原图 SHA-256 不变。
- v0.8.3 真实 AI 总平图同图回归仍为 734 条原始中心线、556 个 CAD 对象、8 个建筑候选、45 个树木块和 1 个景观椭圆，顶点减少 33.7%，未出现回退。该图停车位是开放分隔线，因此保守规则不强行块化。
- 图转 CAD、GUI、内存与兼容专项 46/46 通过；完整回归 136/136 通过。新增覆盖黑底白线自动判断、旋转建筑、停车位块定义/实例和原生停车位块预览。
- 打包清单包含图转 CAD 与原生预览模块；桌面快捷方式指向 v0.8.4 目录版 EXE。全程不需要 API，也未使用界面缩放覆盖参数。

## v0.8.3 新增验证证据

- 规则树木符号不再保留为大量锯齿轮廓：本轮真实 AI 住宅总平图识别出 45 个保守树木候选，建立 1 个 `PT_TREE` 圆形块定义，并在 `BW_TREE_CANDIDATE` 图层输出 45 个 `INSERT`。在 CAD 中修改块定义即可统一调整全部树木，原生预览也会展开为圆形而不是十字占位符。
- 对大尺度、平滑且椭圆度足够高的封闭轮廓进行保守拟合；真实测试图中央景观识别为 1 个真正的 DXF `ELLIPSE`，输出至 `BW_LANDSCAPE_CANDIDATE`。该逻辑不会把任意自由曲线强行改成椭圆。
- 真实 AI 测试图采用用户明确输入的场地宽度 240 m，不猜测比例。734 条原始中心线最终整理为 556 个 CAD 对象，其中包含 8 个建筑候选、45 个树木块实例和 1 个景观椭圆。
- 多段线顶点由原始识别阶段的 9,523 个降至 6,314 个，减少 3,209 个（33.7%）；相较 v0.8.2 的 9,221 个顶点再减少 2,907 个（31.5%）。DXF 从 348,650 bytes 降至 268,406 bytes，缩小约 23.0%。
- 分层结果为 `BW_LINEWORK` 102、`BW_CLOSED` 4、`BW_DETAIL` 396、`BW_BUILDING_CANDIDATE` 8、`BW_TREE_CANDIDATE` 45、`BW_LANDSCAPE_CANDIDATE` 1；DXF 重新读取审计为 0 errors / 0 fixes，`$INSUNITS=6`。
- 2 px 容差视觉指标为 precision 0.9949、recall 0.8999、F1 0.9450。相较 v0.8.2 的 F1 0.9570 略降，是将树冠锯齿轮廓简化为可编辑圆块、将景观轮廓拟合为椭圆的预期取舍；不能据此宣称测量级精度。
- 图转 CAD 与内存/兼容专项测试 17/17 通过；完整回归 132/132 通过。新增覆盖树木块定义与实例数量、椭圆图元拟合、原生预览块展开。
- 打包清单确认包含 `planning_toolbox.cad.planning.image_to_dxf` 与 `planning_toolbox.gui.widgets.preview_parser`；桌面快捷方式指向新版目录版 EXE。原始测试 PNG SHA-256 仍为 `9BF1E92CE67EFB169DF5DA8D85AE22766A77BD1D61F5C01401361CC511C0F1BF`，转换前后不变。

## v0.8.2 新增验证证据

- 黑白中心线增加“自动整理线条”：按端点邻近和方向连续性安全配对，在 T/X 分叉处优先保持直行轮廓并保留支路，避免把整张线网盲目连接成一个对象。
- 从封闭白色内部提取保守的矩形建筑候选，输出到 `BW_BUILDING_CANDIDATE`；同时按编辑用途分为 `BW_LINEWORK`、`BW_CLOSED`、`BW_DETAIL` 和 `BW_FRAME`。候选名称和界面警告明确禁止将其当作已确认真实建筑。
- 新增 GUI 默认开启的“自动整理线条”开关，参数可随 `.ptx` 项目保存恢复；结果区显示原始/整理后线数、减少数量和建筑候选数。
- 真实 AI 测试图（1536×1024、8 栋住宅）从 734 条原始中心线整理为 565 个输出对象，减少 169 个（约 23.0%）；识别 8 个闭合建筑候选，与输入已知数量一致。
- 增强输出包含 107 条主线、46 条闭合线、404 条细节线和 8 个建筑候选；DXF 重新读取审计为 0 errors / 0 fixes，`$INSUNITS=6`，8 个候选全部闭合，候选面积约 354.1–395.1 m²。
- 2 px 容差视觉指标：precision 1.0000、recall 0.9175、F1 0.9570；说明优化优先改善编辑结构，仍保留较高视觉相似度，但不能据此宣称测量级精度。
- 图转 CAD 专项自动测试 7/7 通过；新增覆盖短段整理、建筑闭合保护、小树符号不误判和原图 SHA-256 零修改。
- 打包清单确认包含 `planning_toolbox.cad.planning.image_to_dxf`、`skimage.measure` 与 `skimage.morphology`；桌面快捷方式仍指向新目录版 EXE。

## v0.8.1 新增验证证据

- 无界面渲染基线中的空闲源码 GUI 实测约 58.2 MB RSS；Matplotlib、Excel/PDF、ezdxf、Shapely 与图像识别栈均改为按需加载。正常桌面渲染会额外加载 Qt/显卡线程，源码版稳定约 156.1 MB。
- 主 CAD 预览由 Matplotlib 改为 Qt 原生矢量画布；载入示例 DXF 后约 98.3 MB RSS、11 个图元正常绘制，且确认 `matplotlib` 未加载。相对 v0.8.0 的 124.8 MB 再降低约 26.5 MB（约 21%）。
- 原生画布支持滚轮缩放、左键拖动、双击/按钮适应窗口，并可将完整场景导出最高边 2400 像素 PNG；方案叠加和修复差异模块仍保留原有绘图实现。
- 复杂 ARC/CIRCLE/ELLIPSE/SPLINE/INSERT/TEXT 等图元增加预览与兼容性清点；面积计算仍只接受拓扑验证的闭合多段线。
- 质量结果新增修复前后红/绿差异高亮；DWG 支持通过本机 ODA File Converter 转为新 DXF，不依赖 API、不覆盖源文件。

| 能力 | 自动验证结果 |
|---|---|
| 同图层重复 LINE 删除 | PASS |
| 无分叉 LINE / 开放 LWPOLYLINE 链合并 | PASS |
| 分叉路口安全跳过 | PASS |
| 共线冗余点和极短段清理 | PASS |
| 中文/英文图层别名统一 | PASS |
| `$INSUNITS=0` 深度整理阻断 | PASS |
| 每项修改 CSV 追踪 | PASS |
| 源 DXF SHA-256 保持不变 | PASS |

### 内存与重复启动验证

- 无缓存全量测试：128 项通过。
- v0.8.1 桌面 EXE 启动 15 秒后空闲内存约 144.1 MB；相对 v0.8.0 的 171.2 MB 再降低约 27.1 MB（约 16%），相对 v0.7.0 的约 221.7–222.2 MB 累计降低约 35%。
- 第二次启动在 0.40 秒内退出；主窗口响应正常并可正常关闭，残留进程为 0。
- 打包归档已确认包含 CAD 预览、修复对比、方案对比、Excel/PDF、图转 CAD、DWG 桥接等全部延迟模块。
- 当前测试电脑未安装 ODA File Converter；DWG 缺少组件的中文引导测试通过，真实 DWG 转换留待安装该本机组件后验证。
- 测试使用关闭 pytest 缓存的隔离临时目录；完成后删除临时数据与缓存。

用户行为模拟图位于 `test_artifacts/minimum_manual_v070_20260808/`：原始 12 个图元整理为 6 个，7 个碎片合并为 2 条多段线，删除 1 条重复 LINE，修复 1 个近闭合轮廓，清理 5 个冗余顶点，统一 12 个图层归属，并安全跳过 1 个分叉组件；共记录 18 项可追溯修改。

---

## Historical RC1 Evidence

> 历史证据文件：对应 RC1，不代表当前 `v0.5.0` 工作树。当前 Codex 接手后的修复与 63 项测试结果见 [CODEX_REMEDIATION_REPORT.md](CODEX_REMEDIATION_REPORT.md)。

## Overall 状态
**RC1 / PASS WITH LIMITATIONS**  
(所有已测 CLI/Python 空间算法、 Fail-Safe 单位校验、SHA-256 零破坏防护及 DXF 生成 100% 真实通过；AutoCAD / ArcGIS Pro 手工 GUI 验证严格标记为 NOT TESTED / PENDING，禁止标记最终 STABLE)

---

## 1. 强制自检问答 (Rule 22 Audit Checklist)

1. **真实运行过代码吗？**  
   YES — 已在 PowerShell 命令行真实调用执行 Python 模块与测试脚本。
2. **真实运行过 pytest 吗？**  
   YES — 运行命令：`python -m pytest -v` (Exit Code: `0`)
3. **完整 pytest 命令是什么？**  
   `python -m pytest -v`
4. **Exit code 是多少？**  
   `0`
5. **真实 DXF 是否参与测试？**  
   YES
6. **DXF 属于哪种类型？**  
   `SAMPLE` (`sample_data/sample_parcels.dxf`) 与 `SYNTHETIC` (测试导出的规范几何 DXF)
7. **输出 DXF 是否重新读取验证？**  
   YES — 使用 ezdxf 重新读取 `output/sample_parcels_labeled.dxf` 并验证图层与 MTEXT 数量。
8. **是否在 AutoCAD 中人工验证？**  
   NO — 当前 CLI 执行环境中无 AutoCAD 进程驱动。按照 Rule 14 保持 `AutoCAD manual validation: NOT TESTED / PENDING`，等待用户双查。
9. **是否存在未验证能力？**  
   YES — AutoCAD 手工图形呈现/字体双查（`NOT TESTED`）、ArcGIS Pro 兼容性（`NOT TESTED`）。
10. **是否存在推测性结论？**  
    YES — 假设未声明单位且配置了显式 `fallback_unit: m` 的 DXF 图纸单位为米。已标记为 `ASSUMPTION`。

---

## 2. 核心修正点与真实证据

### A. 未知 DXF 单位 Fail-Safe 生产默认策略
- **规则**：`$INSUNITS` 可识别则正常处理；`$INSUNITS` 未知 (0) 且未显式配置 `fallback_unit` 时，系统**禁止静默默认 metre**，严格触发 `UnitError` 并中断执行（状态标记为 `BLOCKED`）。
- **回归测试**：`tests/test_units.py::test_unspecified_dxf_unit_failsafe_blocked` 真实通过 (Exit Code: 0)。

### B. 原始 DXF “零破坏” SHA-256 完整性哈希
- **测试文件**：`sample_data/sample_parcels.dxf`
- **处理前 SHA-256**：`36bee4289a84aef39f13366e504b8f4ad83e71bc2bf20ca10821a56651426446`
- **处理后 SHA-256**：`36bee4289a84aef39f13366e504b8f4ad83e71bc2bf20ca10821a56651426446`
- **结论**：SHA-256 哈希值 $100\%$ 完全一致，证明原始 DXF 文件未受任何修改。

### C. 性能测试数据来源明确标注
- **1000 synthetic simple parcels benchmark**：$0.886\text{ s}$（明确标注数据来源于程序生成的简单正方形网格地块，**严禁泛化为真实复杂 CAD 图纸性能**）。

---

## 3. 数值精确度与误差分析 (Rule 9)

- **GS-001 ($100\text{m}\times 100\text{m}$ 正方形面积)**:
  - Expected: $10000.00\,\text{m}^2$ | Actual: $10000.00\,\text{m}^2$ | Absolute Error: $0.00\,\text{m}^2$ | Tolerance: $\pm 0.01\,\text{m}^2$ | Status: **PASS**
- **GS-002 ($200\text{m}\times 50\text{m}$ 矩形面积)**:
  - Expected: $10000.00\,\text{m}^2$ | Actual: $10000.00\,\text{m}^2$ | Absolute Error: $0.00\,\text{m}^2$ | Tolerance: $\pm 0.01\,\text{m}^2$ | Status: **PASS**
- **GS-003 ($5\text{m}$ 退界内部区域面积)**:
  - Expected: $8100.00\,\text{m}^2$ | Actual: $8100.00\,\text{m}^2$ | Absolute Error: $0.00\,\text{m}^2$ | Tolerance: $\pm 0.01\,\text{m}^2$ | Status: **PASS**
- **Bulge 弧线地块 ($90^\circ$ 弧段)**:
  - Expected: $8573.39\,\text{m}^2$ | Actual: $8573.39\,\text{m}^2$ | Absolute Error: $<0.01\,\text{m}^2$ | Tolerance: $\pm 0.10\,\text{m}^2$ | Status: **PASS**

---

## 4. Git 证据记录 (Rule 6)
- **Branch**: main
- **Commit Hash**: `b1c71c075bcf6716d9d6c0b40b95577cc120b032`
- **Working Tree**: Clean
- **Tags**: `v0.1.0-mvp1`, `v0.1.1-optimized`, `v0.2.0-cad-layers`

---

## 5. 证据文件归档
完整矩阵见 [TEST_EVIDENCE_MATRIX.md](file:///c:/AutoOS/OS1/TEST_EVIDENCE_MATRIX.md)，控制台原始输出存放在 `test_artifacts/latest/`。
