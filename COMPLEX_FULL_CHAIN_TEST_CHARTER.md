# Planning Toolbox 复杂全链路测试章程

## 目的

这套测试不用于证明“软件能跑通”，而是用于揭示它在真实城乡规划课程作业中的能力边界。每个场景都按一名非编程用户的路径执行：建立项目 → 选择 GIS/CAD/图片资料 → 检查与标准化 → 计算/生成 → 导出 CAD → 导入 SketchUp → 保存证据。

**判定原则：** 安全阻断、明确提示“需要人工复核”、或保留原始可编辑数据，都属于正确行为；为了看起来完整而捏造道路、建筑高度、规范结论，属于失败。

## 统一输入与验收规则

- 坐标：项目主数据为米制 `EPSG:4547`，启用本地近原点；另设 WGS84 与超大坐标的预期阻断案例。
- 文件：每个案例都保存原始文件 SHA-256、工作 DXF、`.ptx`、指标 CSV/Excel、SketchUp handoff、SKP 和两张 PNG。
- 图层：使用显式语义，例如 `PARCEL`、`ROAD_CENTERLINE`、`ROAD_12M`、`GREEN`、`PARKING`、`BUILDING_RES_6F_FH3.0_FLAT`。
- 结果等级：`PASS`（达到量化目标）、`SAFE-BLOCK`（不安全输入被阻断）、`REVIEW-REQUIRED`（诚实保守地交给人工）、`PRODUCT-GAP`（当前系统没有足够能力）。
- 不以单一截图判断：同时检查 CAD 对位、对象数量、指标、质量报告、SketchUp 分组和真实人工修正时间。

## 场景矩阵

| ID | 场地复杂度与输入 | 主要验证能力 | 当前预期 | 暴露的边界 |
|---|---|---|---|---|
| C01 | 4.5 ha 不规则居住地块；12 栋不同层数建筑、2 处公共服务设施、3 片绿地、180 个停车位 | GIS 导入、分层指标、逐栋楼层/用途、CAD→SU 分组 | `PASS/REVIEW-REQUIRED` | 不会从普通轮廓猜测建筑用途或高度；必须显式图层或逐栋设置 |
| C02 | 主路、支路、消防环路、2 个弧形转角、3 处交叉口、4 个不同角度斑马线 | 道路中心线、宽度、斑马线方向、路灯/人行道构件 | `PASS`（显式中心线） | 闭合且凹形的“道路面”不能可靠推断车道与路缘；系统应降级为基础道路面 |
| C03 | 2 个相连地块；目标 FAR、密度、绿地率、停车位均有明确上下限 | 概念生成器能否真正满足指标目标 | **预期 `PRODUCT-GAP`** | 现有生成器可能少放建筑，未达到目标覆盖率时不会自动迭代或强制阻断 |
| C04 | 真实感“脏 CAD”：重复线、断线、自交、多段弧、块参照、空层、异常比例、中文标注 | 质量检查、可选修复、原图只读、复杂图元保留 | `PASS/REVIEW-REQUIRED` | 不能自动替代人工判断图层语义；修复必须生成新 DXF，不得改原图 |
| C05 | 黑白总平面底图 + 语义引导图；含阴影、树木、图框、文字、曲线建筑和不完整道路 | 图片→CAD 候选、人工复核、底图锁定、候选拒绝后不建模 | `REVIEW-REQUIRED` | 图片无法保证得到工程精度；低置信度对象不能直接变成建筑/道路 |
| C06 | WGS84 GeoJSON、EPSG:3857、未设置近原点的 500 km 坐标、DXF 单位未知 | CRS/单位/近原点安全门禁 | `SAFE-BLOCK` | 系统只应提示投影转换路径，不能自行假设面积、退线或三维尺度 |
| C07 | 250 栋、1,000 停车位、40 条道路段、300 个绿化对象的大型课程住区 | 内存、时间、对象预算、增量导入、可用性 | `PASS/REVIEW-REQUIRED` | 立面、道路家具和树木必须受预算限制；低配置机器的耗时需要实测而非承诺 |
| C08 | 一张真实课程 CAD 或从 ArcGIS Pro 导出的多图层 GPKG，含用户已绘制局部方案 | 真实资料接力、图层映射、人工修改时间、输出可继续编辑 | 以实测判定 | 这是最终可信度案例；不使用合成结果替代真实课程图 |

## 各场景的量化验收

### C01：混合居住组团

- 输入至少 12 栋建筑，其中包含 4/6/9/12 层和居住、商业、公共服务三类用途。
- CAD→SU：建筑对象数 = 12；每栋都保留稳定 `PT-*` 身份；高度与显式层数误差为 0。
- 指标：地块面积、建筑基底面积、总建筑面积、FAR、密度、绿地率和停车位全部可导出。
- 人工复核：建筑功能、入口方向、屋顶形式、公共空间品质不应被宣称为“自动正确”。

### C02：道路与交通组织

- 每一条要细化的道路必须提供 `ROAD_CENTERLINE` 与宽度来源；每处斑马线有明确位置。
- 检查道路端点覆盖率、斑马线与最近道路局部切线夹角、交叉口构件重叠和道路组标签。
- 验收阈值：中心线端点误差不超过 0.01 m；斑马线方向采用“条纹平行于车辆行驶方向”规则；不确定的路口应标记复核而不是硬生成。
- 特意放入一条凹形闭合环路：它只允许生成基础道路面，不允许产生穿越绿地的虚假细节。

### C03：目标指标达标压力

- 为两个地块分别设定 FAR、密度、绿地率、建筑数、最小退线、最小间距和停车位目标。
- 将“要求值、实际值、差值、未达标原因”放进结果表。
- 当前预期发现：概念布局会在可放置空间不足时少放建筑，覆盖率可能显著低于目标。这是待开发的“布局迭代/目标不达标阻断”能力，不应掩盖。

### C04：脏图与质量修复

- 原始 DXF SHA-256 在检查前后相同。
- 质量报告分别列出重复线、断线、自交、空层、图元类型、比例异常和不可安全修复项。
- 仅在用户勾选后生成修复副本；对曲线、块、文字、尺寸和未知实体保留“需人工核对”记录。

### C05：图片到 CAD 的保守性

- 检查候选建筑、道路、绿地、停车各自的数量和置信状态。
- 手工拒绝 20% 的错误候选后，SketchUp 中被拒绝对象必须只作为锁定参考底图，不得变成自动模型。
- 对复杂曲线、阴影、文字和图框造成的误识别，记录为算法误差，不靠截图掩盖。

### C06：坐标与单位门禁

- EPSG:4326 / EPSG:3857 / DXF `$INSUNITS=0` 必须阻断面积、退线和 SketchUp 精确建模。
- 阻断提示必须告诉用户下一步怎么做：在 ArcGIS Pro 投影到本地米制 CRS、设置 DXF 单位、设定近原点。
- 这些案例“被阻断”即通过；若产生数值结果则为严重失败。

### C07：性能与内存

- 在记录机器配置后，分别记录 DXF 检查、指标、质量检查、handoff 导出、真实 SketchUp 导入的耗时与峰值内存。
- 输出对象预算命中情况：立面实例、道路构件、路灯、停车、树木。
- 通过标准不是追求无限细节，而是在可接受时间内保持界面响应、无崩溃、无孤儿对象，并能保存可编辑 SKP。

### C08：真实作业验收

- 选用一份允许用于个人学习的真实课程图；保留原图只读，并记录人工开始/结束时间。
- 比较“纯手工完成”与“使用工具生成后复核”的耗时；分别记录 CAD 清理、指标、图层整理、SU 基础建模和最后修图时间。
- 成功标准：工具减少重复劳动且不降低用户对成果的控制；不以“完全无需人工”作为目标。

## 必须输出的证据包

每个案例放入独立目录，至少包含：

1. `01_input/`：原始 GIS/CAD/图片与 SHA-256 清单；
2. `02_working_cad/`：标准化、修复、概念方案的副本；
3. `03_results/`：指标 CSV、Excel、质量报告和 `.ptx`；
4. `04_sketchup/`：`.ptsu.json`、RBZ、真实导入 `.skp`、顶视图与轴测图；
5. `05_evaluation.md`：实际值、预期值、截图、人工修正分钟数、`PASS/SAFE-BLOCK/REVIEW-REQUIRED/PRODUCT-GAP` 结论。

## 建议的执行顺序

先跑 C06（确保不会做错事），再跑 C02 与 C03（最能暴露当前道路和指标生成边界），然后跑 C01 与 C04，最后用 C07 评估性能、C08 做真实作业验收。这样得到的结论会比单一小地块模型更可信。

