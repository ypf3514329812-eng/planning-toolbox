# Planning Toolbox 开源发布与产品优化计划书

> 计划基线：Planning Toolbox `0.58.1`  
> 制定日期：2026-08-11  
> 项目目录：`C:\AutoOS\OS1`  
> 目标：以安全、合规、可复现的方式发布 GitHub 公开 Beta，并逐步形成符合 Codex for Open Source 申请要求的活跃开源项目。

## 一、结论与发布定位

Planning Toolbox 已具备公开展示和公开测试价值，但当前工作树不能原样上传。建议首个公开版本定位为：

**“面向城乡规划学生与小型研究工作的本地优先 CAD–GIS–SketchUp 自动化工具箱（Public Beta）”。**

首发不宣称：

- 法定规划审批工具；
- 自动替代规划师或设计师；
- 任意图片都能准确恢复原始 CAD；
- 任意复杂 DWG/DXF 都能无损转换；
- 自动生成施工图或审查级 SketchUp 模型。

首发核心价值应集中为：

1. 减少面积统计、指标核算、图层整理、图纸检查等重复工作；
2. 通过单位、坐标系和 SHA-256 检查降低误算与源文件破坏风险；
3. 建立 CAD–GIS–SketchUp 可复核的语义接力；
4. 为没有编程基础的城乡规划学生提供中文图形化操作入口；
5. 保留人工专业判断，不把机器候选冒充最终设计成果。

## 二、当前基线

| 项目 | 当前状态 |
|---|---|
| 应用版本 | `0.58.1` |
| 自动化测试 | `262/262 PASS` |
| 实机验证 | ArcGIS Pro、SketchUp 2026、RBZ、Windows EXE 已验证 |
| 源码体积 | `src` 约 3.75 MiB |
| 测试代码 | 约 1.84 MiB |
| 合法内置素材 | 约 0.26 MiB，已有 CC0 来源文件 |
| SketchUp RBZ | 约 0.96 MiB |
| Windows 目录包 | 约 641 MiB，不应提交 Git 历史 |
| 生成测试证据 | 约 210 MiB、505 个文件，不应整体公开 |
| Git 状态 | 33 个已修改文件、630 个未跟踪文件 |
| 版本一致性 | Git HEAD 仍为 `0.6.0`，工作树为 `0.58.1` |
| 开源治理文件 | LICENSE、CI、贡献指南、安全政策等尚缺失 |
| GitHub Remote | 尚未配置 |

## 三、总体执行阶段

| 阶段 | 优先级 | 建议周期 | 交付目标 |
|---|---:|---:|---|
| R0 发布冻结与资产审计 | P0 | 1–2 天 | 明确公开范围，冻结新增功能 |
| R1 合规与仓库清理 | P0 | 2–4 天 | 可安全提交的干净仓库 |
| R2 工程化与可复现安装 | P0 | 3–5 天 | CI、安装、测试、版本一致 |
| R3 首发功能收口 | P1 | 1–2 周 | 完成高价值稳定性优化 |
| R4 文档、演示与公开 Beta | P1 | 3–5 天 | GitHub 仓库与首个 Release |
| R5 用户反馈与持续维护 | P1 | 持续 | 形成真实使用与维护证据 |
| R6 Codex for Open Source 申请 | P2 | 有使用证据后 | 提交有数据支持的申请 |

功能冻结期间只处理发布阻断、稳定性、隐私、兼容性和文档问题，不继续扩张大型模块。

## 四、R0：发布冻结与公开范围

### 4.1 公开内容

- `src/planning_toolbox/`：产品源码；
- `tests/`：可公开、可复现、使用合成数据的测试；
- `scripts/`：通用构建和验证脚本；
- `sample_data/`：完全自制、无个人信息的最小样例；
- `assets/`：许可明确、体积受控的素材及原始许可证；
- 精简后的 README、架构、贡献、测试和许可证文档；
- PyInstaller spec、SketchUp 插件源码和 RBZ 构建逻辑。

### 4.2 默认不公开内容

- `build/`、`dist/`、`output/`；
- `test_artifacts/` 全量过程证据；
- `.skb`、缓存、日志、临时目录和本机 MCP 配置；
- 用户提供的图片、CAD、GIS、SKP、作业成果及其衍生文件；
- 来源不明确的标准全文、扫描件、截图和模型；
- 本机绝对路径、用户名、Token、API Key 和组织 ID。

### 4.3 发布历史策略

1. 先对现有 Git 历史做敏感信息和大文件扫描；
2. 若历史安全，保留历史并建立 `release/oss-v0.58.1` 分支；
3. 若历史含用户资料、凭据或不明资产，建立新的干净公开仓库；
4. 不伪造提交历史，不把数月工作拆成虚假日期提交；
5. 首个公开提交可以明确命名为 `Initial public beta release`。

## 五、R1：许可证、隐私与第三方合规

### 5.1 项目许可证决策

由项目所有者最终选择：

- 推荐 `Apache-2.0`：适合长期工程协作，包含明确专利授权条款；
- 备选 `MIT`：文字简单、采用门槛低，适合个人学习项目。

完成后在 `pyproject.toml`、README 和源代码发布信息中保持一致。

### 5.2 必须新增的合规文件

- `LICENSE`；
- `THIRD_PARTY_NOTICES.md`；
- `NOTICE`（选择 Apache-2.0 时）；
- Qt/PySide6 LGPLv3 使用声明和完整许可证文本；
- CC0 组件来源、原始文件名、许可证、转换过程和 SHA-256 清单；
- 国家标准及地方规范仅引用名称、编号和公开元数据，不复制受版权保护的完整正文。

### 5.3 发布安全检查

- 扫描源码、文档、Git 历史和待发布附件中的密钥；
- 扫描 `C:\Users\...`、真实姓名、邮箱、学校、项目位置等个人信息；
- 检查 DXF/GeoJSON/SKP 元数据和图签；
- 检查测试图片是否来自用户作业、网络案例或付费素材；
- 生成可审计的第三方依赖清单或 SBOM；
- 发布前重新验证 MCP 只监听 `127.0.0.1`，使用随机令牌和路径白名单。

## 六、R2：仓库工程化与可复现性

### 6.1 清理和目录规范

- 扩充 `.gitignore`，明确排除根目录生成物；
- 把必要测试证据压缩为小型合成黄金样例；
- 将长篇审计报告移动到 `docs/audit/`，README 只保留入口；
- 将用户教程、开发者文档、架构文档和验证证据分目录管理；
- 移除或参数化 `C:\AutoOS\OS1`、固定 SketchUp 安装路径等本机假设；
- 所有运行目录使用项目发现、环境变量或用户选择，不猜测个人目录。

### 6.2 GitHub 基础设施

新增：

- `.github/workflows/ci.yml`；
- `.github/ISSUE_TEMPLATE/bug_report.yml`；
- `.github/ISSUE_TEMPLATE/feature_request.yml`；
- `.github/pull_request_template.md`；
- `CONTRIBUTING.md`；
- `CODE_OF_CONDUCT.md`；
- `SECURITY.md`；
- `CHANGELOG.md`；
- `ROADMAP.md`；
- `ARCHITECTURE.md`；
- `CITATION.cff`。

### 6.3 CI 验证矩阵

每次提交至少执行：

1. Windows + Python 3.10、3.11、3.13 核心测试；
2. Ubuntu 上运行不依赖 Windows/ArcGIS/SketchUp 的核心计算测试；
3. 代码格式、静态检查和包构建；
4. 敏感信息与超大文件检查；
5. 示例 DXF 的零修改 SHA-256 回归；
6. RBZ 结构、版本和资产哈希验证；
7. PyInstaller 构建作为发布工作流，不在每个普通提交重复执行。

ArcGIS Pro、SketchUp 和 AutoCAD 属于许可软件或桌面应用，保留为版本发布前的人工/本机验收，不在公共 CI 中伪造通过。

### 6.4 版本与发布纪律

- 应用、Python 包、MCP、SketchUp 插件和 RBZ 使用同一版本源；
- 每个 Release 对应不可变 Git Tag；
- Release Notes 区分新增、修复、破坏性变化和已知限制；
- 记录 EXE/RBZ SHA-256；
- 保证干净环境执行 `pip install .`、启动 GUI 和运行示例成功。

## 七、R3：首发前功能优化清单

### 7.1 P0：必须完成

#### A. 首次启动与环境自检

- 第一次启动显示中文欢迎向导；
- 自动检查 Python 包环境、ArcGIS Pro、SketchUp、ODA File Converter 和输出目录权限；
- 清楚区分“必须组件”和“可选增强”；
- 一键打开合成示例项目；
- 错误提示只展示用户可执行的解决办法，详细堆栈写入日志。

验收标准：无编程背景用户可以在 10 分钟内完成示例 DXF 的检查、指标计算和成果导出。

#### B. 路径与配置可移植性

- 移除 `C:\AutoOS\OS1` 默认硬编码；
- MCP 根目录从当前项目、配置或明确环境变量获取；
- SketchUp 安装位置采用检测和用户选择；
- 项目移动到其他盘符后 `.ptx` 能提示并重新定位文件。

验收标准：将仓库复制到另一 Windows 用户和盘符后，核心功能无需修改源码即可运行。

#### C. 原始数据与失败恢复

- 所有修改继续只写新文件；
- 中途取消、崩溃和磁盘不足不能留下伪成功成果；
- 临时文件使用项目级临时目录并在完成后清理；
- 每次结果记录输入哈希、参数、版本、状态和输出清单。

#### D. 结果可信度

- 图片转 CAD、道路识别、建筑识别统一显示置信度和待复核数量；
- 结果页明确区分“自动结果”“人工确认”“规范辅助”“法定结论”；
- 阻止低置信道路直接生成高完成度 SU 实体；
- 面积、FAR、退线和坐标转换输出可追溯报告。

### 7.2 P1：高价值功能增强

#### A. 图转 CAD 最低返工工作流

- 加强道路中心线、路口、弯道、环岛和建筑闭合轮廓的候选分离；
- 在现有语义编辑器中增加候选接受、拒绝、拆分和合并；
- 提供原图、候选、修正结果三层对照；
- 为建筑、道路、绿地、水体、停车分别给出召回率式统计和人工复核清单；
- 不追求不可解释的端到端黑箱，优先保持可编辑、可撤销、可追溯。

#### B. CAD 兼容与质量修复

- 增加 ARC/SPLINE/ELLIPSE、嵌套块、属性块和 3D 图元的黄金样例；
- 强化断线、重复线、自交、异常比例和空图层检测；
- 修复动作分为安全自动修复、建议修复、必须人工处理；
- DWG 继续通过用户本机 ODA/AutoCAD 转换，不把专有运行时打包进项目；
- 增加 DXF 导入—处理—重新打开的往返一致性检查。

#### C. CAD → SketchUp 建模质量

- 优先完善道路连续性、交叉口、转弯半径、斑马线局部切线和设施避让；
- 增强建筑用途、楼层、屋顶、入口和立面细节的可控预设；
- 保持组件复用和实例预算，避免模型对象爆炸；
- 继续使用增量更新和手工锁定，保护用户已精修对象；
- 增加模型完成度报告：建筑、道路、绿化、停车、设施、待复核对象数量；
- MCP 增加会话状态和任务级日志，但继续禁止任意 Ruby 和任意命令执行。

#### D. GIS–CAD–SU 坐标契约

- 将项目 CRS、CAD 单位、近原点偏移和旋转集中管理；
- 每次转换执行可逆性和单位校验；
- 明确阻断经纬度直接量算和未经确认的 EPSG:3857；
- 输出坐标谱系，方便从 SketchUp 结果追溯到 GIS/CAD 来源。

#### E. 多方案对比与作业成果包

- 对比面积、FAR、密度、绿地率、停车和模型对象数量；
- 增加图形差异叠加和变化对象清单；
- 一键导出 Excel、PDF、PNG、DXF、GeoJSON、`.ptsu.json` 和项目说明；
- 成果包自动写入软件版本、输入哈希、参数和非审批免责声明。

### 7.3 P2：发布后迭代

- 地方规范模板插件化，不把各地规则硬编码进核心；
- 支持社区贡献规则模板和合成测试样例；
- 研究 CityJSON/IFC 等开放三维交换，但不在首发阶段扩张；
- 提供可选组件资源包，核心安装保持轻量；
- 建立不上传用户图纸的本地质量评估机制；
- 根据真实 Issue 决定是否开发更复杂地形、路口和景观模块。

## 八、性能与体积目标

| 指标 | 当前参考 | 首发目标 |
|---|---:|---:|
| 源码与核心小型资源 | 约 6 MiB | 不超过 15 MiB |
| SketchUp RBZ | 约 0.96 MiB | 不超过 5 MiB |
| 建模知识与组件预算 | 低于 100 MB | 继续保持硬上限 |
| Windows 目录包 | 约 641 MiB | 首阶段低于 450 MiB，长期目标低于 300 MiB |
| 空闲 GUI 内存 | 既有测试约 139–156 MiB | 不高于 160 MiB |
| 示例项目首屏可操作 | 待统一量化 | 普通电脑 8 秒内 |
| 大任务取消 | 已有后台线程 | 取消后 5 秒内回到可操作状态 |

减重原则：延迟导入、排除未使用二进制、可选功能分包和组件按需加载；不得通过删除用户可见功能伪造减重结果。

## 九、测试与质量门禁

### 9.1 自动化测试

- 保留现有 262 项作为回归基线；
- 新功能必须同时新增正向、失败和边界测试；
- 建立核心模块覆盖率基线，再逐步达到 80%，不以无意义 GUI 行数追求数字；
- 固定随机种子和几何容差，避免偶发失败；
- 所有样例使用合成数据并记录来源。

### 9.2 五类发布测试

1. 单元测试：几何、单位、CRS、规则、指标；
2. 契约测试：DXF、GeoJSON、项目文件、语义 sidecar、`.ptsu.json`；
3. 集成测试：GUI Worker、批处理、报告导出、MCP；
4. 真机测试：ArcGIS Pro、SketchUp、ODA/AutoCAD；
5. 用户任务测试：从新建项目到交付成果包的完整流程。

### 9.3 首发阻断条件

任何一项发生则不得发布：

- 测试失败；
- 源 DXF/图片/GIS 文件哈希发生变化；
- 单位未知却继续面积或退线计算；
- 经纬度被当作米制坐标；
- 发布包包含用户文件、密钥或来源不明素材；
- MCP 对外网卡监听或允许任意代码执行；
- LICENSE、第三方声明或版本号缺失；
- 干净 Windows 环境无法完成示例流程。

## 十、README 与演示设计

README 首屏只回答五个问题：

1. 这是什么；
2. 能替我节省哪些工作；
3. 不能替我做什么；
4. 如何在五分钟内运行；
5. 去哪里看教程、下载和反馈问题。

建议公开演示材料：

- 一张四区域桌面界面截图；
- 一个 60–90 秒无声流程 GIF/视频；
- 一组完全合成的“原始 DXF → 检查 → CAD 整理 → GIS → SU”成果；
- 图转 CAD 的原图、候选、人工修正三图对比；
- 量化前后操作步骤和耗时，不使用未经验证的夸张百分比；
- 中英文简短简介，详细教程以中文为主。

## 十一、公开 Release 方案

### 首个公开版本

建议名称：`v0.58.1-public-beta`。

发布内容：

- GitHub Source Archive；
- SketchUp RBZ；
- 小型合成示例数据；
- 安装与五分钟教程；
- 已知限制；
- 测试摘要和 SHA-256；
- Windows EXE 暂缓到 LGPL 通知、体积和干净电脑验证完成后发布。

### Release 验收清单

- [ ] Git 状态干净；
- [ ] GitHub Remote 指向正确公开仓库；
- [ ] LICENSE 和第三方声明完整；
- [ ] CI 全绿；
- [ ] 版本号全部一致；
- [ ] README 链接、截图和安装命令可用；
- [ ] 全新环境安装通过；
- [ ] 262 项及新增测试全部通过；
- [ ] RBZ 在 SketchUp 2026 安装、导入、保存和退出通过；
- [ ] 发布包不含用户数据；
- [ ] Release Notes 明确 Beta 和已知限制。

## 十二、社区与维护机制

### Issue 标签

- `bug`；
- `cad`；
- `gis`；
- `sketchup`；
- `image-to-cad`；
- `documentation`；
- `good first issue`；
- `needs-sample`；
- `needs-professional-review`；
- `privacy-security`。

### 维护承诺

- 每周至少一次 Issue 分类；
- 重大数据安全或源文件破坏问题优先处理；
- 每个 Release 发布兼容性与已知问题；
- 不要求用户公开上传正式项目图纸；
- Bug 样例优先通过脱敏或最小合成数据复现；
- 规划规范类贡献必须附来源、适用地区、版本和非审批说明。

## 十三、Codex for Open Source 申请准备

官方申请为滚动审核，仓库和 GitHub 个人资料必须公开。申请前建议形成以下真实证据：

- 至少一个可下载的公开 Beta Release；
- 连续维护记录和版本更新；
- 外部规划学生或教师的实际试用反馈；
- 可核实的 Release 下载、Stars、Forks、Issue 和讨论数据；
- 至少三条完整任务的人工耗时对比；
- 清楚说明 Codex 用于 PR 审查、测试生成、问题分类、发布自动化或安全检查；
- OpenAI Organization ID 和 API 额度使用方案。

不得购买 Stars、伪造用户、虚构下载量或编造节省时间。

建议申请定位：

> Planning Toolbox 是面向城乡规划教育和小型研究工作的本地优先 CAD–GIS–SketchUp 自动化项目。它通过单位/坐标安全门、源图零修改、可追溯指标计算、图转 CAD 复核流程和 SketchUp 自动建模，降低非编程规划学生使用专业空间工具的门槛，并把人工时间集中到规划判断而非重复操作。

官方申请入口：<https://openai.com/zh-Hans-CN/form/codex-for-oss/>

## 十四、风险登记

| 风险 | 等级 | 控制措施 |
|---|---:|---|
| 用户图纸或个人信息误上传 | 极高 | 默认排除生成物；发布前历史和待提交文件双重扫描 |
| 规范辅助被误解为审批结论 | 极高 | GUI、报告、README 三处明确免责声明和人工复核要求 |
| 第三方资产或 Qt 许可证不合规 | 高 | LICENSE、LGPL 文本、第三方声明、SBOM、逐件资产清单 |
| 图片识别错误进入 SU | 高 | 置信度门、待复核层、人工接受/拒绝和结果谱系 |
| Windows EXE 过大 | 中高 | 可选分包、排除未用 MKL/科学计算二进制、Release 外置 |
| 单维护者负担过大 | 中高 | 冻结范围、Issue 模板、贡献指南、模块负责人规则 |
| SketchUp/ArcGIS 版本变化 | 中 | 兼容矩阵、适配层、真实发布前验收 |
| MCP 本地控制被滥用 | 中 | 回环监听、随机令牌、命令白名单、路径白名单、无任意代码 |
| 功能持续膨胀 | 中 | 核心/可选插件分层，每个新增模块必须说明用户收益和体积成本 |

## 十五、最终完成定义

只有同时满足以下条件，项目才视为“适合公开发布”：

1. 公开内容无用户数据、凭据和不明来源资产；
2. LICENSE、第三方声明和 Qt 分发义务完成；
3. Git 工作树干净、版本统一、CI 全绿；
4. 全新 Windows 环境可按 README 完成安装和示例任务；
5. 核心输出可追溯，源文件零修改门继续有效；
6. README 对能力和局限的描述与实际一致；
7. 至少一套合成 CAD–GIS–SU 全链路演示可公开复现；
8. 发布包具备 Release Notes、已知限制和 SHA-256；
9. GitHub Issues、贡献与安全反馈入口可用；
10. 项目明确标记 Public Beta，不冒充法定审批或生产级设计软件。

完成 R0–R4 后，预计 GitHub 公开发布准备度可由当前约 4/10 提升到 8/10。Codex for Open Source 的入选概率仍主要取决于后续真实使用量、生态价值和持续维护记录，而不是单纯的功能数量。
