# Architecture Overview

Planning Toolbox 采用本地优先、分层和可追溯设计。

```text
GUI / CLI
   │
   ├── 项目与任务编排（.ptx、批处理、结果清单）
   ├── CAD 核心（ezdxf、几何、单位、拓扑、指标）
   ├── GIS 适配层（GeoJSON 内置；ArcGIS/QGIS/GDAL 可选外部桥）
   ├── 图片语义层（候选、置信度、人工修正、来源指纹）
   ├── 规则与知识卡（可追溯、非审批、按需加载）
   └── SketchUp 交接（.ptsu.json、RBZ、MCP 回环桥）
```

## 不变量

- 原始 DXF、图片和 GIS 输入只读，修改写入新文件；
- 单位未知、地理坐标或不安全 CRS 不得静默进入精确计算；
- 机器候选必须保留置信度和人工复核状态；
- MCP 只提供固定工具，不提供任意 Ruby、Shell 或远程命令执行；
- 组件和规则使用明确来源、许可证、版本和体积预算；
- SketchUp 模型中的对象应保留来源 ID、几何指纹和必要的谱系元数据。

## 可选依赖边界

ArcGIS Pro、QGIS/GDAL、ODA/AutoCAD 和 SketchUp 均属于用户本机的可选能力，不打包进核心 Python 环境。核心功能应在没有这些软件时给出明确提示，并继续支持不依赖它们的 DXF、GeoJSON、指标和质量检查流程。

## 发布前架构检查

- 不允许业务模块直接读取用户目录；
- 不允许 GUI 线程执行长时间几何计算；
- 不允许测试依赖本机绝对路径；
- 不允许把生成证据、用户文件或大型二进制写入源码包；
- schema、版本和输出格式变化必须有迁移或兼容说明。
