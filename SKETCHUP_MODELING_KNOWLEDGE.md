# Planning Toolbox 轻量级 SketchUp 建模知识库

## 目标

本知识库用于帮助 Planning Toolbox 把已经整理、分类和定标的 CAD 线稿更稳定地交接为可编辑 SketchUp 规划模型。它不是机器学习训练集，不保存网上图片、纹理或模型权重；运行时读取小型规则 JSON，并按需加载少量经过许可、尺寸和指纹校验的原生 SKP 组件，不需要 API。

当前规则文件：`src/planning_toolbox/knowledge/sketchup_modeling_rules.json`

当前规则版本：`2026.08.6`

当前组件目录：`src/planning_toolbox/knowledge/sketchup_component_catalog.json`

当前组件目录版本：`2026.08.10`

## 规则怎样参与建模

1. **模型精度**：快速体量、课程作业、汇报模型分别使用不同的立面组件预算、场地细节和树木分段数。
2. **建筑类型**：居住、办公、商业、校园和通用建筑分别使用可编辑的立面模数、窗尺寸、入口宽度、阳台资格和低饱和颜色。
3. **建筑细节**：按楼层与层高生成基座、入口、雨棚、楼层线、女儿墙或简单坡屋顶；汇报模式可增加有限数量的住宅阳台和屋顶设备。
4. **场地表达**：道路、停车、绿地和水体使用克制的视觉高差与薄层；根据精度增加路缘、标线、绿地收边或水岸。
5. **道路横断面**：可选择自动、完整街道、基础车行道或关闭细化。闭合且接近矩形的道路可生成双侧人行道、两条边线、中心虚线、双向箭头和共享街灯；短端不生成横向路缘，避免把道路端部封死。不规则道路保持 CAD 原轮廓。多段弯道只在两侧边界局部平行、宽度稳定时生成局部切线提示，不强行把整条弯道矩形化。
6. **弯道实体细化**：当多段弯道通过局部帧可信度检查时，SketchUp 插件沿帧序列分段生成连续的人行道、边线、中心虚线和方向箭头；这些几何只在道路局部帧覆盖范围内生成，不延伸到原始 CAD 边界之外。缺少帧、宽度不可信或路段过短时仅保留 CAD 原面，并把结果留给人工复核。
7. **弯道设施方向**：可信弯道还会按局部切线在道路两侧布置街灯，记录局部站点、方向角和道路侧别，并沿用过街避让区；若组件缺失、路段过短或预算耗尽则只保留道路几何，不阻断交接。
8. **圆弧、样条和环岛**：`ROAD_CENTERLINE` / `ROAD_AXIS` / `CENTERLINE` 以及对应的 `ROUNDABOUT_CENTERLINE` 可安全提供 ARC/SPLINE 的局部切线方向；其宽度始终标为概念估计。默认只用于方向辅助；用户明确勾选“中心线生成概念道路带”后，才会按最多 64 个局部帧生成可编辑的概念车行道、边线、中心虚线、方向箭头和街灯，并把几何置信度与宽度来源写入交接文件。明确命名为 `ROUNDABOUT` / `环岛` 的圆形曲线才会生成环带道路，使用有限数量局部帧、保留中央岛空腔，不把 CIRCLE 填成实心圆。环岛不生成直行中心虚线，边线和斑马线沿环向切线匹配。
9. **道路设施方向**：普通 `PT_CROSSWALK` 会优先匹配可信矩形道路、弯道局部切线或环岛局部切线，斑马线长条与车辆行驶方向平行，并根据车行道宽度调整横跨距离。匹配成功后，中心虚线、方向箭头和自动街灯会避让过街区。七条白色标线由插件生成独立薄实体，避免外部组件道路底板或隐藏网格干扰。`PT_CROSSWALK_FIXED` / `PT_CROSSWALK_MANUAL` 保留 CAD 指定角度；交叉口歧义、宽度不可信、异形道路和无法匹配时也保留 CAD 角度并标记待复核。交通灯始终使用 CAD 明确角度，不自动猜测信号控制。
10. **植被**：根据 CAD 树木符号范围推断树冠尺度，量化尺寸后复用共享组件，并提供小范围确定性旋转与缩放变化。
11. **性能保护**：立面和重复构件有明确上限；每条道路最多 2 条人行道、2 条边线和 4 个方向箭头，并把箭头避开常见的道路中心过街区；弯道和环岛候选最多保留 64 个局部帧，曲线交接最多保留 256 个采样点，树木、街灯和道路设施复用组件定义，避免课程场地因自动细化无限膨胀。

## 可复用组件库怎样工作

当前内置 14 个精选组件，总计 1,220,468 bytes（约 1.16 MB），低于 100 MB 资源包硬预算。除 CC0 组件外，停车车辆、座椅、灌木组、隔离柱和公交候车亭还提供项目原生过程式生成器；RBZ 在第一次遇到对应对象时才加载或生成定义，随后所有同类对象共享该定义，不会为每辆车、每个座椅复制一套网格。

参考知识索引收录 22 类规划建模场景。索引只保存来源页面、参考图检索词、几何生成要点和复核清单，不嵌入图片、纹理或模型权重，因此不会把大量图片拖入运行内存。原生组件和参考索引都不需要 API 或网络连接。

| 组件 | 触发方式 | 当前用途 | 安全回退 |
|---|---|---|---|
| 大树 / 小树 | `PT_TREE`、`TREE_SYMBOL` 等树木块 | 按 CAD 树冠尺寸选择大小并确定性旋转/缩放 | 原程序化低多边形树 |
| 入口挑檐 / 雨棚 | 有高度的建筑体量 | 按居住、办公、商业、校园类型自动选择并匹配入口宽度 | 原程序化雨棚 |
| 街灯 | 汇报模型中的规则四边形道路或可信多段弯道 | 直路沿两条长边、弯道沿局部切线按 18 m 间距有上限地布置，并避让过街区 | 不影响道路与标线生成 |
| 花池 | 块名 `PT_PLANTER` / `PLANTER` / `花池` / `树池` | 用 CAD 块中心定位共享花池 | 保留原块线稿 |
| 遮阳伞 | 块名 `PT_PARASOL` / `PARASOL` / `UMBRELLA` / `遮阳伞` | 用 CAD 块中心定位共享场地构件 | 保留原块线稿 |
| 斑马线道路块 | 自动块名 `PT_CROSSWALK` / `PT_ROAD_CROSSING` / `CROSSWALK` / `斑马线` / `人行横道`；手动块名 `PT_CROSSWALK_FIXED` / `PT_CROSSWALK_MANUAL` | 自动块匹配最近可信道路，使长条平行车行方向并按车行道宽度缩放；手动块保留 CAD 角度 | 歧义、异形或未匹配道路保留 CAD 角度并列入待复核；不会自动假设路口 |
| 交通灯 | 块名 `PT_TRAFFIC_LIGHT` / `TRAFFIC_LIGHT` / `SIGNAL_LIGHT` / `交通灯` / `信号灯` / `红绿灯` | 按 CAD 块位置与旋转角度放置共享交通灯 | 保留原块线稿；不会自动判断信号控制方式 |

加载器会逐件核对 SKP 文件名、大小和 SHA-256。SketchUp 端只接受目录内的安全文件名，组件损坏或缺失时不会中断整个项目。组件被加载后会写入来源、许可证和组件编号，并应用统一的低饱和规划模型材质。

需要更精致的组件时，可在 SketchUp 的“扩展程序”菜单选择“打开 Planning Toolbox 自定义组件文件夹”。把自己的轻量 SKP 按说明文件改成对应文件名后，用户组件会优先于内置组件；系统仍负责匹配目标尺寸、方向和地面基点。删除自定义文件即可恢复内置组件，不需要修改代码，也不会增大 Planning Toolbox 主安装包。

## 已审核的组件来源

| 来源 | 许可证 | 当前状态 | 选择理由 |
|---|---|---|---|
| Kenney City Kit Roads / Suburban / Commercial | CC0-1.0 | 精选 7 个组件已内置 | 单件小、风格统一、适合重复实例化 |
| Shapespark Low Poly Exterior Plants | CC0-1.0 | 候选，不随软件打包 | 植物更自然但整套模型和纹理较大，需逐件拆分与双面材质复核 |
| Quaternius CC0 Packs | CC0-1.0 | 候选 | 格式齐全，需逐包核对规划场景适用性 |
| KayKit City Builder Bits | CC0-1.0 | 精选斑马线道路块和交通灯 2 个组件已内置 | 32 个以上城市构建低多边形模型；本轮仅取道路表达直接需要的两件，原 GLTF 由无依赖本地工具封装为 GLB 后经 SketchUp 2026 转为 SKP |

没有整包复制 3D Warehouse 模型，因为其许可通常需要逐模型核对，且未经筛选的高面数模型容易拖慢学生电脑。

## 优先级与安全边界

`用户逐栋设置 > 项目全局设置 > 地方规则模板 > 轻量知识默认值`

- 楼层、层高、坐标系、项目原点、退界和地方规范参数不能由知识库猜测。
- 规则中的尺寸是可编辑的建模启发值，不是国家或地方强制性条文。
- 自动模型用于作业底稿、方案比较和汇报辅助；审批成果、施工表达和复杂设计判断仍需人工复核。
- 所有来源均记录用途、许可证和是否复制代码；当前版本没有复制第三方业务代码。

## 开源与标准参考矩阵

| 来源 | 许可证/性质 | 采纳内容 | 未采纳内容 |
|---|---|---|---|
| OGC CityGML 3.0 | OGC 开放标准，参考使用 | 城市对象语义、多细节层级、建筑/道路/植被/水体分层 | 完整 CityGML 数据库与交换实现 |
| CityJSON Specifications | CC0-1.0 | 紧凑 JSON、版本化元数据、城市对象语义字段 | 完整 CityJSON 编解码器 |
| OSM2World | MIT | 二维语义到三维对象的程序化建模思路、按对象类型选择表达 | Java 运行时、完整 OSM 转换器、纹理资源 |
| SketchUp Ruby API Tutorials | MIT | 官方扩展结构、原生可编辑几何、事务式生成 | 与本项目无关的示例扩展 |
| SketchUp Entity Overview | 官方文档，参考使用 | 重复对象用组件、独立对象用组隔离、曲面以多面体近似 | 未公开授权的代码或资源 |
| 3D City Database | Apache-2.0 | 多尺度对象组织、语义与几何分离、未来 GIS 三维交换预留 | PostgreSQL/PostGIS 服务、完整城市数据库 |
| Godot Road Generator | MIT | 道路点定义横断面、车道/路肩分离、道路段按需生成、每段独立性能预算 | Godot 运行时、GDScript、交通仿真和完整程序化路口；本项目未复制代码 |
| Streetmix | AGPL-3.0-or-later | 用普通用户能理解的方式组合车行道、人行道、绿化和街道设施 | AGPL Web 代码、Node.js 服务和素材；仅参考交互概念，不复制代码 |
| KayKit City Builder Bits | CC0-1.0 | 斑马线道路块、交通灯低多边形组件及原始纹理 | 未整包复制其余模型，避免软件和 RBZ 膨胀 |
| GB 5768.3-2025《道路交通标志和标线 第3部分：道路交通标线》 | 中国强制性国家标准，元数据与语义参考 | 使用现行标准编号、生效状态与道路标线语义约束知识边界 | 不把教学模型参数标记为国标合规，不替代正式条文核验和工程审查 |
| FHWA MUTCD 11th Edition Part 3 | 美国官方交通控制设施标准，方法参考 | 高可见性人行横道纵向条带与来车行驶路径平行、尺寸与间距保持统一的方向语义 | 不把美国尺寸直接套用于中国项目，不作为中国规范结论 |
| Truevision Designer | 自定义非商业 EULA | 仅参考其道路、车道、路口、标线和设施工具分类 | 未复用代码、资产或运行时；许可证不适合作为本项目内置代码来源 |

## 后续扩展方式

新增规则时应满足以下要求：

- 先记录来源、许可证、采纳理由和可信度，再增加参数；
- 优先增加可解释的小规则，不引入常驻深度学习模型；
- 新组件必须逐件记录源文件、许可证、SHA-256、原始/目标尺寸和实例预算，不允许直接整包塞入；
- 新规则必须有边界、性能预算和自动化测试；
- 中国国家标准或地方规范应放入独立、可选择的规则模板，并记录标准编号、版本、适用地区和生效状态；
- 规范模板不得覆盖用户明确输入，也不得把学习辅助结果标记为合规结论。
